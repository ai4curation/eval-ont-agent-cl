## Summary

Added a new CL term for issue #3196:

- `CL:9900001` dual-feature fallopian tube progenitor cell

## Rationale

The issue thread settled on "dual-feature fallopian tube progenitor cell" as the primary label, with UCFP/unclassified names retained as synonyms. I added the reviewed human fallopian tube progenitor concept as a new `CL_99xxxxx` term because no matching label or synonym was present in `src/ontology/cl-edit.owl`.

The term is defined as a human fallopian tube epithelium progenitor with epithelial and endothelial marker features and potential to differentiate into fallopian tube ciliated and secretory epithelial cells. The logical definition uses:

- `progenitor cell` (`CL:0011026`)
- `part of` some `fallopian tube epithelium` (`UBERON:0003889`)
- `in taxon` some `Homo sapiens` (`NCBITaxon:9606`)
- `develops into` some `fallopian tube secretory epithelial cell` (`CL:4030006`)
- `develops into` some `fallopian tube multiciliated epithelial cell` (`CL:4030007`)

## Checklist

- Checked `src/ontology/cl-edit.owl` for existing matching terms and synonyms.
- Checked the proposed parent/context terms already present in CL.
- Checked DOSDP patterns for relevant compositional/taxon patterns.
- Tried `aurelian fulltext PMID:40475517`, but `aurelian` is not installed in this environment.
- Checked publication metadata using NCBI E-utilities for PMID:40475517 and Europe PMC/NCBI metadata for PMID:35320732.
- Added contributor ORCID `0000-0002-1482-1032`, creator `GitHub Copilot`, date metadata, and the issue tracker annotation.
- Ran `git diff --check`.

## Validation

`git diff --check` passed. Full ROBOT/OWL validation was not run because neither `robot` nor `owltools` is installed in this environment.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951028390)
