## Summary

Adds three abbreviation synonyms requested in issue #3559 to support text-to-ontology mapping of cell-type abbreviations commonly found in experimental sample metadata.

| Term | ID | Abbreviation | Synonym scope |
|---|---|---|---|
| peripheral blood mononuclear cell | CL:2000001 | PBMC | exact (abbreviation) |
| leukocyte | CL:0000738 | WBC | exact (abbreviation) |
| retinal pigment epithelial cell | CL:0002586 | RPE | exact (abbreviation) |

## Rationale

In the issue thread:

- @addiehl confirmed that `PBMC` and `WBC` "can probably be safely added as exact synonyms".
- @scheuerm confirmed that `RPE` would be appropriate to add as a synonym for retinal pigment epithelial cell.

Following the requester's stated need (LLM-extracted cell-type strings that failed to map to CL), these abbreviations were registered as exact synonyms (`oboInOwl:hasExactSynonym`) annotated with `OMO:0003000` (abbreviation synonym type), consistent with existing patterns in the ontology (e.g. `HSC`, `MSC`, `APC`, `D2-MSN`, etc.).

Notes on `WBC` specifically: the leukocyte term already has `"white blood cell"` as an exact synonym, so adding the corresponding abbreviation `"WBC"` is consistent.

## Checklist

- [x] Confirmed CL IDs by grep in `src/ontology/cl-edit.owl`
- [x] Verified existing convention for abbreviation synonyms uses `oboInOwl:hasSynonymType obo:OMO_0003000`
- [x] Verified that combining `OMO:0003000` with `hasExactSynonym` is an existing pattern in the file
- [x] Edited only `src/ontology/cl-edit.owl`
- [x] Committed on branch `issue-3559`

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/26011656239)
