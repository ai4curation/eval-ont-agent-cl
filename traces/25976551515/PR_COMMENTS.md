## Summary

This change addresses issue #3588 by removing local relabeling of imported `oboInOwl:*` annotation properties from `src/ontology/cl-edit.owl`.

It also adds a custom Makefile guard, `test_no_oboInOwl_labels`, to fail routine tests if an `AnnotationAssertion(rdfs:label oboInOwl:...)` axiom is reintroduced into `cl-edit.owl`.

## Rationale

The `oboInOwl:*` annotation properties are imported and already labeled by imported modules. Keeping local labels in `cl-edit.owl` can cause spurious serialization diffs, especially for properties such as `hasDbXref` that may have conflicting imported labels.

## Checklist

- Removed all `AnnotationAssertion(rdfs:label oboInOwl:...)` axioms from `src/ontology/cl-edit.owl`.
- Added `test_no_oboInOwl_labels` to `src/ontology/cl.Makefile`.
- Included the new guard in the custom `test` dependencies.
- Verified no matching `oboInOwl` label assertions remain in `cl-edit.owl`.
- Ran `make test_no_oboInOwl_labels` successfully.
- Ran `git diff --check` for the edited files.

Signed-off-by: GitHub Copilot

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25976551515)
