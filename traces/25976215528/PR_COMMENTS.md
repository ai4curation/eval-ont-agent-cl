Addresses issue #3536 by adding epithelial shape axiomatization for squamous and cuboidal epithelial cells.

Changes made:

- Added new term `cuboidal epithelial cell` (`CL_9900000`) with definition, issue tracker link, creation date, creator, and logical definition as `epithelial cell` and `has characteristic` some `cuboid` (`PATO:0001872`).
- Updated `squamous epithelial cell` (`CL_0000076`) with a textual definition and logical definition as `epithelial cell` and `has characteristic` some `flattened` (`PATO:0002254`).
- Added direct `has characteristic` shape restrictions to selected squamous and cuboidal epithelial cell terms whose labels or definitions explicitly state these shapes.
- Linked selected cuboidal epithelial descendants to the new `cuboidal epithelial cell` parent where appropriate.
- Added `epithelial cell` as a stated parent for `salivary gland glandular cell` so the cuboidal salivary duct descendant has an explicit epithelial lineage.
- Extended `docs/relations_guide.md` with guidance for recording epithelial cell shape using `has characteristic` and PATO terms.

Rationale:

- The issue requested making squamous and cuboidal epithelial shapes explicit in logical definitions and in stated restrictions for relevant terms.
- `PATO:0002254` (`flattened`) and `PATO:0001872` (`cuboid`, synonym cuboidal) were checked in OLS before use.
- The requested DOSDP files under `src/patterns` were not added because the local repository instructions restrict edits to `src/ontology/cl-edit.owl` and `docs/`.

Validation:

- Ran `robot convert -i src/ontology/cl-edit.owl -o /tmp/opencode/cl-edit-test.owl` successfully.
- Ran `git diff --check -- src/ontology/cl-edit.owl docs/relations_guide.md` successfully.

Checklist completed:

- Checked existing epithelial terms and shape axioms.
- Checked proposed parent terms for consistency.
- Added ontology axioms for squamous and cuboidal epithelial shape patterns.
- Updated documentation for epithelial shape characteristics.
- Validated ontology syntax.

Signed-off-by: GitHub Copilot

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25976215528)
