## Summary

This addresses issue #3224 by excluding `skos:prefLabel` from the merged import pipeline and removing the currently checked-in `skos:prefLabel` annotations from `src/ontology/imports/merged_import.owl`.

## Rationale

MBAO imports were bringing in `skos:prefLabel` annotations, including annotations on CL classes. These duplicate labels can cause Protégé edits, such as obsoletion edits, to target the imported merged import artifact instead of `cl-edit.owl`. Since CL already maintains its own labels, excluding `skos:prefLabel` avoids the redundant imported annotations and prevents that editing workflow interference.

## Changes

- Added `<http://www.w3.org/2004/02/skos/core#prefLabel*>` to `import_group.exclude_iri_patterns` in `src/ontology/cl-odk.yaml`.
- Regenerated `src/ontology/Makefile` so the merged import recipe removes matching `skos:prefLabel` IRIs before extraction.
- Removed existing `skos:prefLabel` declarations/assertions from `src/ontology/imports/merged_import.owl`.

## Completed Checklist

- Read the imported issue context from `__issue_context__.json`.
- Confirmed `src/ontology/imports/merged_import.owl` contained `skos:prefLabel` annotations, including on CL classes.
- Confirmed `src/ontology/cl-edit.owl` did not contain `MBAO`/`skos:prefLabel` content requiring a native CL class edit.
- Updated the import configuration rather than editing CL term axioms.
- Verified `src/ontology/imports/merged_import.owl` no longer contains `skos:prefLabel`.
- Ran `robot convert -i src/ontology/imports/merged_import.owl -o /tmp/cl-issue-3224-merged_import.ofn`.
- Ran `make -C src/ontology config_check`.
- Ran `git diff --check` on the edited files.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25640302868)
