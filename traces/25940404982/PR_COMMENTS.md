# Add `add_by_HRA` subset tag

Addresses obophenotype/cell-ontology#3590.

## Summary of changes

Adds a new subset annotation property `cl:add_by_HRA` to `src/ontology/cl-edit.owl` so the HRA team can tag terms they (or the HuBMAP team) contribute, enabling provenance/tracking queries against the cell ontology.

The new subset follows the same shape as the existing `cl:added_for_HCA` subset, which is already used for the HCA-contributed term subset.

## What was added

In the Declarations block (line ~3592):

```
Declaration(AnnotationProperty(cl:add_by_HRA))
```

In the Annotation Properties block (line ~3643):

```
# Annotation Property: cl:add_by_HRA (add_by_HRA)

AnnotationAssertion(obo:IAO_0000233 cl:add_by_HRA <https://github.com/obophenotype/cell-ontology/issues/3590>)
AnnotationAssertion(terms:date cl:add_by_HRA "2026-05-15T00:00:00Z"^^xsd:dateTime)
AnnotationAssertion(rdfs:comment cl:add_by_HRA "a subset of terms added by the Human Reference Atlas (HRA) and HuBMAP team members, used for tracking purposes.")
AnnotationAssertion(rdfs:label cl:add_by_HRA "add_by_HRA")
SubAnnotationPropertyOf(cl:add_by_HRA oboInOwl:SubsetProperty)
```

## Rationale / design notes

- **IRI**: `http://purl.obolibrary.org/obo/cl#add_by_HRA` (the `cl:` prefix), matching the convention used by `cl:added_for_HCA`, `cl:BDS_subset`, `cl:kidney_upper_slim`, etc. The local name `add_by_HRA` is taken verbatim from the issue title to avoid second-guessing the requester.
- **`SubAnnotationPropertyOf … oboInOwl:SubsetProperty`**: This is what makes the property usable as a value of `oboInOwl:inSubset` and what causes ROBOT/ODK to treat it as a subset. Same pattern as the other `cl:*` subset properties in the file.
- **`obo:IAO_0000233` (term_tracker_item)**: Links the subset back to issue #3590. Per the `CLAUDE.md` "Other metadata" section, new entities should link to the originating issue.
- **`terms:date`**: Creation date stamp, following the project convention for new entities.
- **`rdfs:comment` + `rdfs:label`**: Match the pattern used by `cl:blood_and_immune_upper_slim` and `cl:kidney_upper_slim`, which both have a date + comment + label.

## Checklist

- [x] Confirmed the request: new subset property, not a new class, so no `CL_99xxxxx` ID needed.
- [x] Reviewed the existing `cl:added_for_HCA` definition and its usage (`oboInOwl:inSubset obo:CL_0008033 cl:added_for_HCA`, etc.) to mirror the structure.
- [x] Added `Declaration(AnnotationProperty(...))` in the Declarations block.
- [x] Added the property section in the Annotation Properties block with `SubAnnotationPropertyOf(... oboInOwl:SubsetProperty)`.
- [x] Linked back to issue #3590 via `IAO_0000233`.
- [x] Stamped `terms:date`.
- [x] Used label `add_by_HRA` verbatim from the issue title.
- [x] Only edited `src/ontology/cl-edit.owl`, per the project guide.

## Open questions for reviewers

- The issue uses `add_by_HRA`, but the existing related subset is named `added_for_HCA`. If consistency is preferred over verbatim matching, `added_for_HRA` would be the natural mirror. Happy to rename if the HRA team prefers that.
- No existing terms have been tagged yet; the HRA team can apply `oboInOwl:inSubset obo:CL_NNNNNNN cl:add_by_HRA` to the relevant terms in follow-up PRs (or I can pre-populate from a provided list).

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25940404982)
