# Add abbreviation synonyms PBMC, WBC, RPE

Addresses issue #3559.

## Summary

Three commonly-used abbreviations are added as **exact synonyms** with synonym type `OMO:0003000` (abbreviation) to the corresponding Cell Ontology terms. This supports text-mining and LLM-based extraction workflows (such as the one described in the issue) that need to map abbreviations found in experimental sample metadata to CL terms.

## Changes

| Term | ID | Added synonym | Scope | Type |
|---|---|---|---|---|
| peripheral blood mononuclear cell | CL:2000001 | PBMC | exact | abbreviation (OMO:0003000) |
| leukocyte | CL:0000738 | WBC | exact | abbreviation (OMO:0003000) |
| retinal pigment epithelial cell | CL:0002586 | RPE | exact | abbreviation (OMO:0003000) |

## Rationale

- **Synonym scope (exact):** The reporter requested addition of these abbreviations. @addiehl (MEMBER) confirmed PBMC and WBC could be added as exact synonyms. @scheuerm (COLLABORATOR) confirmed RPE is appropriate as a synonym for retinal pigment epithelial cell. Each of these abbreviations refers unambiguously to the named term, so "exact" is the appropriate scope.
- **Synonym type (`OMO:0003000` abbreviation):** This is the standard pattern already used in CL for abbreviation synonyms (e.g. `HSC` for hematopoietic stem cell, `CMP`, `CLP`, `D2-MSN`, `VEC`, etc.). It marks the synonym as an abbreviation while leaving its scope as exact, so it remains discoverable both by abbreviation-aware tools and by general synonym lookup.
- **Format:** Each new axiom follows the existing pattern in the file:
  ```
  AnnotationAssertion(Annotation(oboInOwl:hasSynonymType obo:OMO_0003000) oboInOwl:hasExactSynonym obo:CL_NNNNNNN "ABBR")
  ```

## Checklist

- [x] Verified target term IDs and current labels via `grep` on `src/ontology/cl-edit.owl`
- [x] Confirmed `OMO:0003000` (abbreviation) is the synonym-type convention used elsewhere in CL
- [x] Confirmed no existing PBMC / WBC / RPE synonyms on the affected terms (no duplicates)
- [x] Followed the requested exact-synonym scope agreed in the issue thread
- [x] Edits limited to `src/ontology/cl-edit.owl` (no other files touched per project guide)
- [x] Did not modify creator metadata on these existing terms (per editor guide)

## Notes

- No new terms are introduced; only synonym annotations are added to three existing terms, so no `dc:date`, `dc:creator`, or ID-range concerns apply.
- The reporter also asked whether there is a general policy on registering abbreviations as synonyms. That is a broader policy discussion that the editors may want to address in the issue thread; this PR only handles the three specific cases that were confirmed by maintainers/collaborators.

Signed-off-by: GitHub Copilot

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25966396540)
