Added a new CL term for issue #3458:

- `fibrochondrocyte progenitor cell` (`CL_9900001`)

Rationale:

- The issue request was clear and supported by the cited literature.
- `PMID:31871141` identifies fibrochondrocyte progenitors (`FCP`) in human meniscus single-cell data and supports progenitor-like behavior plus the requested abbreviation.
- `PMID:36338137` supports the colony-forming and multipotent meniscal progenitor phenotype used in the textual definition.
- The existing ontology already contains `fibrochondrocyte` (`CL_4072104`) and `proliferative fibrochondrocyte` (`CL_0020018`), so this addition fills the missing upstream progenitor term referenced by related meniscus/fibrocartilage content.

Modeling decisions:

- Added subclass axioms to `mesenchymal cell` (`CL_0008019`) and `progenitor cell` (`CL_0011026`).
- Added `part_of some fibrocartilage` using `UBERON_0001995`, matching the anatomical framing already used for `fibrochondrocyte`.
- Added `develops_into some fibrochondrocyte` (`RO_0002202 some CL_4072104`) to capture the requested lineage relationship conservatively from the progenitor side.
- Added `in taxon some Homo sapiens` because the supporting evidence here is human-specific.
- Added the requested ORCID contributor, issue tracker annotation, creation metadata, and related synonym `FCP`.

Validation performed:

- Checked `src/ontology/cl-edit.owl` for existing `mesenchymal cell`, `progenitor cell`, and `fibrochondrocyte` terms before adding anything new.
- Confirmed that `fibrochondrocyte progenitor cell` was not already present in the local ontology file.
- Reviewed the cited literature through available web/PubMed-accessible sources after the local `aurelian` helper was unavailable in this environment.
- Ran `robot convert --input src/ontology/cl-edit.owl --output /tmp/cl-edit.ofn` successfully to confirm syntax.

Completed checklist:

- [x] Read imported issue context
- [x] Verified the requested term was not already present in `cl-edit.owl`
- [x] Checked proposed parents for consistency
- [x] Added definition, synonym, issue link, contributor ORCID, and timestamp
- [x] Validated the edited ontology file
- [x] Committed only the ontology edit locally

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25640324883)
