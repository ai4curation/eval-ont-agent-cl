Thanks for the request! The abbreviation synonyms have been added in PR #<NN>:

- `PBMC` → exact synonym of `peripheral blood mononuclear cell` (CL:2000001)
- `WBC` → exact synonym of `leukocyte` (CL:0000738)
- `RPE` → exact synonym of `retinal pigment epithelial cell` (CL:0002586)

Each synonym is marked with the `abbreviation` synonym type (`OMO:0003000`), following existing CL practice, so downstream consumers can still distinguish them from full-form labels if needed. Following @addiehl's and @scheuerm's recommendations, all three were added as exact synonyms.

To answer your policy question: CL routinely registers widely used abbreviations as synonyms (typically tagged with the `abbreviation` synonym type), so abbreviations like the ones you flagged are welcome additions.

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951216015)
