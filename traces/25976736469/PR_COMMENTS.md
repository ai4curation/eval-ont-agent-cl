Implemented the requested reclassification updates for issue #3239 in `src/ontology/cl-edit.owl`.

Changes made:
- Updated `CL_0000388 tendon cell` to be defined as a `fibroblast` that is `part of` some `tendon`, replacing the previous `fibrocyte`-based classification.
- Revised the `tendon cell` textual definition so the genus now matches the logical definition.
- Reclassified `CL_0002665 otic fibrocyte` from `fibrocyte` to `mesenchymal cell`, following the newer cochlear mesenchyme framing cited in PMID:37720106.
- Updated the `otic fibrocyte` textual definition to match the new parentage and corrected `adaptions` to `adaptations`.
- Added PMID-backed synonyms to `otic fibrocyte`:
  - exact synonym: `cochlear fibrocyte` (PMID:31866825)
  - related synonym: `spiral ligament fibrocyte` (PMID:33193034)

Rationale:
- The issue explicitly requested removal of both classes from under `fibrocyte`.
- `tendon cell` already follows a fibroblast-like usage pattern in the issue discussion, so switching its equivalence and asserted inferred parent to `fibroblast` is the minimal ontology correction.
- For `otic fibrocyte`, the newer literature describes these as cochlear otic mesenchyme-derived cells rather than supporting a fibrocyte parent as the organizing genus. I therefore moved only the direct parent to `mesenchymal cell` and left the broader subclass structure intact.
- I treated `cochlear fibrocyte` as exact, but `spiral ligament fibrocyte` as related, because the issue body notes that the current general term spans both spiral ligament and spiral limbus and that a more precise spiral ligament term should be handled separately.

Validation performed:
- checked the existing axioms for both target classes and their current parents before editing
- reviewed the cited PubMed abstracts for PMID:31866825, PMID:33193034, and PMID:37720106
- ran `robot convert --input src/ontology/cl-edit.owl --output /tmp/opencode/cl-edit.ofn.owl` to confirm the edited ontology parses successfully

Completed checklist:
- [x] read imported issue context
- [x] updated only `src/ontology/cl-edit.owl`
- [x] kept the scope to the two classes requested here
- [x] validated syntax with ROBOT
- [x] committed only the ontology file

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25976736469)
