## Summary

Adds abbreviation synonyms requested in issue #3559 for three widely used cell-type abbreviations so that downstream string-to-ontology mapping pipelines (e.g., LLM-extracted strings from experimental sample metadata) can resolve them to the appropriate CL term.

## Changes

In `src/ontology/cl-edit.owl`:

| Term | ID | Synonym added | Scope | Type |
|------|----|---------------|-------|------|
| peripheral blood mononuclear cell | `CL:2000001` | `PBMC` | exact | `OMO:0003000` (abbreviation) |
| leukocyte | `CL:0000738` | `WBC` | exact | `OMO:0003000` (abbreviation) |
| retinal pigment epithelial cell | `CL:0002586` | `RPE` | exact | `OMO:0003000` (abbreviation) |

Each affected class also got a `term_tracker_item` (`IAO:0000233`) annotation linking back to issue #3559.

## Rationale

- **Synonym scope (exact)**: The reporter and @addiehl recommended `PBMC` and `WBC` as exact synonyms. @scheuerm confirmed `RPE` is appropriate for retinal pigment epithelial cell; given that `RPE` is a direct shorthand of the full label, exact is the most consistent choice. All three abbreviations are unambiguous and fully synonymous shorthand for their respective labels in the biological literature.
- **Synonym type (`OMO:0003000`)**: The Ontology of Metadata's "abbreviation" synonym type is already used 300+ times in `cl-edit.owl` for similar shorthand forms (e.g. `D2-MSN`, `iSPN`, `COP`, `GBC`, `VIC`, `HSC`, `CFU-E`). Following the precedent of those existing exact-synonym + abbreviation-type annotations.
- **No `hasDbXref` on the synonym**: The abbreviations are well-established community usage rather than coined in a specific publication; existing CL abbreviation synonyms with no xref (e.g. `HSC` on `CL:0000037`, `COP` on `CL_4023059`) follow the same pattern.

## Checklist

- [x] Read issue #3559 and comments
- [x] Verified the three target term IDs and their existing labels/synonyms
- [x] Checked existing abbreviation-synonym precedent in `cl-edit.owl`
- [x] Added `PBMC`, `WBC`, `RPE` as `hasExactSynonym` with `OMO:0003000` synonym type
- [x] Added `term_tracker_item` linking each class to issue #3559
- [x] No existing logical axioms or labels modified
- [x] Committed on branch `issue-3559`

Signed-off-by: GitHub Copilot

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/26012063877)
