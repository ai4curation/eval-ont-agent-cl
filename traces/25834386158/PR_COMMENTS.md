# Remove stale `skos:prefLabel` axioms from `merged_import.owl`

Fixes #3224.

## Problem

The reporter observed that obsoleting a term in Protégé using the standard CL protocol caused the modification to be written to `src/ontology/imports/merged_import.owl` instead of `src/ontology/cl-edit.owl`. The root cause was that the merged import file contained `skos:prefLabel` annotation assertions for many CL terms — imported from the MBAO base ontology — which made the term appear to "live" in both editing contexts and caused Protégé to attach edits to the wrong file.

The reporter confirmed that removing those terms from `merged_import.owl` resolved the editing-target behavior and suggested updating the import pattern so `skos:prefLabel` is excluded from the MBAO import.

## Investigation

1. Counted occurrences in `src/ontology/imports/merged_import.owl`:
   - `Declaration(AnnotationProperty(<http://www.w3.org/2004/02/skos/core#prefLabel>))` — 1
   - `AnnotationAssertion(<http://www.w3.org/2004/02/skos/core#prefLabel> <CL_xxxx> "...")` — 1449
   - Total: 1450 axioms involving `skos:prefLabel`
2. Verified there are no other axiom shapes referencing `skos:prefLabel` in the file (no `SubAnnotationPropertyOf`, no axioms re-annotating it, etc.).
3. Confirmed the only remaining `skos` strings in the file are unrelated literal text values like `"skos:exactMatch"` used as `oboInOwl:source` annotations on GO xref axioms — those are safe to leave in place.
4. Checked the existing import configuration in `src/ontology/cl-odk.yaml`:
   ```yaml
   import_group:
     use_base_merging: TRUE
     annotation_properties:
       - rdfs:label
       - IAO:0000115
       - oboInOwl:is_metadata_tag
       - OMO:0002000
   ```
   This is an allow-list and is materialized in the generated `Makefile` as a `robot remove --select complement --select annotation-properties` step that strips every annotation property not on the list. `skos:prefLabel` is not on the list, so the existing config already covers this case — the checked-in `merged_import.owl` was simply stale relative to the policy.

## Change

Removed exactly 1450 lines from `src/ontology/imports/merged_import.owl`:
- the single `Declaration(AnnotationProperty(<http://www.w3.org/2004/02/skos/core#prefLabel>))` line, and
- all 1449 `AnnotationAssertion(<http://www.w3.org/2004/02/skos/core#prefLabel> <CL_*> "...")` lines.

No other axioms were touched. The file's header, ontology declaration, and closing parenthesis remain intact.

## Validation

- `grep -c "skos/core#prefLabel" src/ontology/imports/merged_import.owl` → `0`
- `git diff --stat` shows `1 file changed, 1450 deletions(-)` (deletions only, no other edits).
- File still opens with the same `Prefix(...)` / `Ontology(...)` preamble and ends with `)`.

## No config change needed

The `import_group.annotation_properties` allow-list in `cl-odk.yaml` already excludes `skos:prefLabel`, so subsequent regenerations of `merged_import.owl` will keep it out. No change to `cl-odk.yaml` or the generated `Makefile` was required.

## Checklist

- [x] Identified all axiom shapes involving `skos:prefLabel` in `merged_import.owl`
- [x] Confirmed no unrelated `skos:` usage would be affected by the removal
- [x] Reviewed `cl-odk.yaml` / `Makefile` to confirm the import policy already excludes `skos:prefLabel`
- [x] Removed only the `skos:prefLabel` axioms (1450 lines, deletions only)
- [x] Verified resulting file has zero `skos:prefLabel` references and is structurally intact
- [x] Committed only `src/ontology/imports/merged_import.owl`

Signed-off-by: GitHub Copilot

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25834386158)
