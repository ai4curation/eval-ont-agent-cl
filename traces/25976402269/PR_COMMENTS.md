This PR addresses issue #3559 by adding exact abbreviation synonyms to the requested Cell Ontology terms:

- `PBMC` as an exact synonym for `peripheral blood mononuclear cell` (`CL:2000001`)
- `WBC` as an exact synonym for `leukocyte` (`CL:0000738`)
- `RPE` as an exact synonym for `retinal pigment epithelial cell` (`CL:0002586`)

Rationale:

- The issue requested these abbreviations to improve ontology mapping of extracted sample metadata strings.
- Curator discussion in the issue supported adding `PBMC` and `WBC` as exact synonyms, and confirmed `RPE` is appropriate as a synonym for retinal pigment epithelial cell.
- Each synonym is annotated with a dbxref to issue #3559, and each touched term has a corresponding `IAO_0000233` tracker annotation.

Checklist completed:

- Checked the existing term stanzas for `CL:2000001`, `CL:0000738`, and `CL:0002586`.
- Checked for existing `PBMC`, `WBC`, and `RPE` synonym usage to avoid duplicates.
- Added only minimal synonym and issue tracker annotations in `src/ontology/cl-edit.owl`.
- Ran `robot convert --input src/ontology/cl-edit.owl --output /tmp/opencode/cl-edit.ofn` successfully.
- Ran `git diff --check` successfully.

Signed-off-by: GitHub Copilot

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25976402269)
