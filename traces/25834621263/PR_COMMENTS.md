# Fix obsolete GO term reference in T follicular helper cell (#2967)

## Summary

Replaces the obsolete `GO:0051024` with `GO:0002639` in the equivalent class axiom for `CL:0002038` (T follicular helper cell).

## Rationale

Issue #2967 reported that the logical definition of `T follicular helper cell` references the obsolete GO term `GO:0051024`:

```
'T follicular helper cell' Equivalent To 'CD4-positive helper T cell' and ('capable of' some GO_0051024)
```

In the issue thread, @addiehl confirmed that the correct replacement, biologically, is `GO:0002639` — which matches the `term replaced by` annotation on the obsolete GO term itself.

## Change

A single edit in `src/ontology/cl-edit.owl` at the `EquivalentClasses` axiom for `CL:0002038`:

- **Before:** `... ObjectSomeValuesFrom(obo:RO_0002215 obo:GO_0051024)))`
- **After:**  `... ObjectSomeValuesFrom(obo:RO_0002215 obo:GO_0002639)))`

No other axioms reference `GO:0051024` in `cl-edit.owl`, so this is the only change needed for the issue.

## Checklist

- [x] Read the linked issue (#2967) and prior thread (#607)
- [x] Confirmed the obsolete term (`GO:0051024`) appears only in the equivalent-class axiom for `CL:0002038`
- [x] Replaced with `GO:0002639` per @addiehl's recommendation and GO's `term replaced_by` annotation
- [x] Verified no other axioms in `cl-edit.owl` reference `GO:0051024`
- [x] Committed change on the issue branch

## Out of scope

The broader request in the thread — adding an automated release-time check for references to deprecated external classes (e.g. via ROBOT's `deprecated_class_reference` standard report) — is not addressed here. That is an ODK/CI configuration change that should be handled in a separate PR.

Signed: GitHub Copilot

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25834621263)
