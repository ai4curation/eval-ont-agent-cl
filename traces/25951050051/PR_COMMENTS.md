Implemented the requested hierarchy and definition updates for the two specific human dendritic cell subclasses discussed in issue #3163:

- `CL_0001057` myeloid dendritic cell, human
- `CL_0001058` plasmacytoid dendritic cell, human

Changes made:

- Added `CD14-negative` to each textual definition.
- Added the requested OMIP references to each definition without removing the existing PMID.
- Added `ObjectSomeValuesFrom(obo:CL_4030046 obo:PR_000001889)` to each equivalent class axiom so the ontology explicitly models lack of CD14 expression.

Deliberate non-change:

- I did not change `CL_0001056` (`dendritic cell, human`). The issue thread records concern that the broader human dendritic cell class should not be constrained as CD14-negative because some human dendritic cell populations do express CD14.

Background checks completed:

- Read the imported issue title, body, and comments from `__issue_context__.json`.
- Verified the existing modeling pattern for negative surface markers directly in `src/ontology/cl-edit.owl`.
- Confirmed that `obo:CL_4030046` is the existing `lacks_plasma_membrane_part` relation and that `obo:PR_000001889` is the CD14 protein term already used elsewhere in the ontology.
- Attempted to use `aurelian fulltext` for PMID follow-up, but `aurelian` is not installed in this environment.
- Attempted formal validation with `robot convert`, but `robot` is also not installed in this environment.

Checklist:

- [x] Read issue context
- [x] Inspected existing axioms for `CL_0001056`, `CL_0001057`, and `CL_0001058`
- [x] Checked the established ontology pattern for negative marker expression
- [x] Updated only the two subclass terms supported by the issue discussion
- [x] Preserved existing definition xrefs while adding the requested PMIDs
- [x] Committed only the ontology edit

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951050051)
