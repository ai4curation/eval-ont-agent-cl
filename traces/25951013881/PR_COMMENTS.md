## Summary

Addressed issue #3346 by updating `src/ontology/cl-edit.owl`.

- Broadened `CL_0002496` (`intraepithelial lymphocyte`) so its logical definition uses `epithelium` (`UBERON_0000483`) rather than `intestinal epithelium`.
- Updated the textual definition of `CL_0002496` to describe tissue-resident mature T cells in epithelial layers with mucosal immune response capability, CD103/integrin alpha-E mediated retention, and activated cytotoxic/innate-like features.
- Retained the existing definition xrefs (`GOC:tfm`, `MP:0008894`) and added the requested `PMID:29674648` and `WIKIPEDIA:Intraepithelial_lymphocyte` xrefs.
- Added `CL_9999999` (`intestinal intraepithelial lymphocyte`) as a new `CL_99xxxxx` term with the requested textual definition, equivalent class axiom, issue tracker annotation, ORCID contributor, creator, and date metadata.

## Rationale

The existing `intraepithelial lymphocyte` equivalent class restricted all IELs to `intestinal epithelium`, while the issue notes IELs occur in multiple epithelial and mucosal tissues. The updated parent term now captures the broader IEL concept, and the intestinal restriction is preserved in the new subclass.

## Checklist

- Read imported issue title/body/comments from `__issue_context__.json`.
- Checked existing `CL_0002496` axioms and relevant IDs in `src/ontology/cl-edit.owl`.
- Checked that `CL_99xxxxx` IDs were not already present in `src/ontology/cl-edit.owl` before assigning `CL_9999999`.
- Tried to fetch the PMID with `aurelian fulltext PMID:29674648`; `aurelian` was not installed.
- Reviewed PubMed/web metadata for PMID:29674648 as fallback.
- Added issue tracker annotations linking both affected terms to issue #3346.
- Ran `git diff --check` successfully.

## Validation

`robot convert` could not be run because `robot` is not installed in this environment. No alternative ontology parser (`owltools`, `rapper`, or `riot`) was available. I did run `git diff --check`, and the local diff contains only the intended ontology file changes.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951013881)
