This change updates the human bipolar neuron terms requested in issue `#3521` with Cell Annotation Platform reference data from `https://celltype.info/project/544/dataset/1157` (`Single cell atlas of the human retina - Bipolar cell subclass - scRNA-seq`).

What changed:
- Added a `database_cross_reference` annotation to the CAP dataset for each requested CL term, annotated with `rdfs:label "reference transcriptomic data on Cell Annotation Platform"`.
- Extended the textual definitions to include the location of the reference transcriptomic data in the standard style, pointing to the CAP dataset and the relevant `cell_type` label.
- Added CAP-based marker-gene comments for each requested term.
- Added `IAO:0000233` issue links back to `https://github.com/obophenotype/cell-ontology/issues/3521` for the edited terms.

Terms updated:
- `CL_4033019` ON-blue cone bipolar cell
- `CL_0000751` rod bipolar cell
- `CL_4033034` invaginating midget bipolar cell
- `CL_4033033` flat midget bipolar cell
- `CL_4033028` diffuse bipolar 2 cell
- `CL_4033027` diffuse bipolar 1 cell
- `CL_0000748` retinal bipolar neuron
- `CL_4033029` diffuse bipolar 3a cell
- `CL_4033031` diffuse bipolar 4 cell
- `CL_4033030` diffuse bipolar 3b cell
- `CL_4033035` giant bipolar cell
- `CL_4033032` diffuse bipolar 6 cell
- `CL_4033036` OFFx cell

Modeling notes:
- The CAP dataset maps `diffuse bipolar 5` to `CL_0000748` because there is no dedicated CL term for DB5 in the issue request. I kept that broader CL class and made the definition/comment wording explicit that the CAP evidence is for a mapped subtype.
- The CAP dataset maps `diffuse bipolar 4 cell` to `CL_4033031`, and its rationale distinguishes `DB4a` and `DB4b` with markers `TTR` and `PRSS12`. I preserved the existing CL term and recorded that distinction in the CAP marker comment instead of creating new terms.
- For ON-blue cone bipolar and giant bipolar cells, the CAP rationale notes shared `PCDH11X` signal; the comments keep the primary distinguishing markers (`SORCS3` and `AGBL1`) and note the shared marker.

Evidence used:
- CAP dataset page: `https://celltype.info/project/544/dataset/1157`
- CAP label pages under that dataset for the listed bipolar labels, including the mapped marker-gene evidence and rationale text.

Completed checklist:
- [x] Read the imported issue context
- [x] Checked the existing CL annotation pattern in `src/ontology/cl-edit.owl`
- [x] Retrieved the CAP dataset and label-level marker evidence
- [x] Updated only `src/ontology/cl-edit.owl`
- [x] Committed only the edited ontology file

Validation:
- `git diff --check -- src/ontology/cl-edit.owl` passed.
- `robot convert` could not be run in this environment because `robot` is not installed.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25949074004)
