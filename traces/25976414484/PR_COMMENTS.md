## Summary

Added explicit mouse taxon constraints to the two requested thymocyte terms:

- `CL_0002423` DN2a thymocyte now has `in taxon: Mus musculus`.
- `CL_0002424` DN2b thymocyte now has `in taxon: Mus musculus`.

Each new axiom is annotated with the originating tracker issue: `https://github.com/obophenotype/cell-ontology/issues/3500`.

## Rationale

The existing term comments already state that DN2a and DN2b thymocytes are observed in mice and caution that double-negative thymocyte markers differ across species. The requested explicit taxon restrictions make that modeling machine-readable and prevent these marker-defined mouse subsets from being interpreted as general cross-species thymocyte classes.

I did not remove `human_subset` annotations because no `human_subset` annotations were present for `CL_0002423` or `CL_0002424` in `src/ontology/cl-edit.owl`.

## Checklist

- Reviewed the imported issue context from `__issue_context__.json`.
- Inspected the DN2a and DN2b entries in `src/ontology/cl-edit.owl`.
- Checked existing `in taxon` patterns using `RO_0002162` and `NCBITaxon_10090`.
- Checked for existing `human_subset` annotations on the affected terms.
- Reviewed the cited PubMed records sufficiently to confirm that the requested mouse restriction is supported, especially PMID:20543111 and PMID:25060579.
- Validated ontology syntax with `robot convert --input src/ontology/cl-edit.owl --output /tmp/opencode/cl-edit-check.owl`.

## Commit

Committed locally as:

`4af1ada Add mouse taxon constraints for DN2 thymocytes`

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25976414484)
