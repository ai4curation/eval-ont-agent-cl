## Summary

This change addresses issue #3521 by adding Cell Annotation Platform reference transcriptomic dataset links to the 13 listed human retinal bipolar neuron terms.

For each listed CL term, I added:

- `rdfs:seeAlso` pointing to `https://celltype.info/project/544/dataset/1157`
- an annotation on that `seeAlso` with `rdfs:label "reference transcriptomic data on Cell Annotation Platform"`
- a term tracker annotation linking back to `https://github.com/obophenotype/cell-ontology/issues/3521`

## Rationale

The issue requested a database/reference link to the Cell Annotation Platform dataset for these terms. I used `rdfs:seeAlso` because the request explicitly specified a `SeeAlso` annotation and the ontology already declares `rdfs:seeAlso`.

I did not add NS-Forest marker axioms because the imported issue context only linked to an external CellMark PR and did not include the specific marker details needed for safe ontology curation.

## Checklist

- Reviewed `__issue_context__.json` for title, body, comments, and target terms.
- Confirmed all listed CL IDs are present in `src/ontology/cl-edit.owl`.
- Checked for existing Cell Annotation Platform dataset annotation patterns in `cl-edit.owl`.
- Added dataset and tracker annotations only to the listed terms.
- Verified 26 expected new annotations are present: 13 dataset links and 13 issue tracker links.
- Ran `robot convert --input src/ontology/cl-edit.owl --output /tmp/opencode/cl-edit-3521.ofn` successfully.
- Committed only `src/ontology/cl-edit.owl` locally.

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25976476470)
