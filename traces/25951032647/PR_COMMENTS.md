## Summary

Added a new CL term for `fasciacyte` using the requested parent `stromal cell` (`CL_0000499`).

## Changes

- Added `CL_9900001` with label `fasciacyte`.
- Added a PMID-backed textual definition using `PMID:29575206` and `PMID:33573365`.
- Added issue tracker metadata linking back to issue 3497.
- Added the requested contributor ORCID `0000-0002-5507-2103`.
- Added `terms:creator "GitHub Copilot"` and a creation timestamp.
- Added an exact plural synonym, `fasciacytes`.
- Asserted `fasciacyte` as a subclass of `stromal cell`.
- Added a `capable of` axiom to `GO_0030213`, following the existing hyaluronan-production pattern used for type B synovial cells.

## Rationale

The issue supplied a clear label, definition, references, parent term, and contributor ORCID. I kept the logical modeling conservative because the issue did not provide a specific UBERON term for deep fascia, and no fascia-related UBERON term was already used in `cl-edit.owl`.

## Checklist

- Confirmed no existing `fasciacyte` term in `src/ontology/cl-edit.owl`.
- Checked the requested parent `CL_0000499` and nearby stromal-cell usage.
- Checked cited publication metadata for the two PMIDs; `aurelian` was not installed, so I used available web/PubMed metadata instead.
- Used a new `CL_99xxxxx` identifier with no existing `CL_99` IDs present in `cl-edit.owl`.
- Ran `git diff --check` successfully.
- Attempted `robot convert`, but `robot` is not installed in this environment.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951032647)
