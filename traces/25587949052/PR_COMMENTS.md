This change updates the MBAO product in `src/ontology/cl-odk.yaml` to use base extraction with an explicit MBAO base IRI:

- `use_base: TRUE`
- `base_iris: https://purl.brain-bican.org/ontology/mbao/`

Rationale:

The reported problem was that obsoleting some CL terms in Protégé could affect `merged_import.owl` because imported MBAO content was bringing CL terms back into the merged import with `skos:prefLabel` assertions. The existing CL import pipeline already intends to keep only a narrow annotation-property set during merged import extraction, so the more robust fix is to constrain the MBAO import itself to the MBAO namespace. That prevents foreign CL-side annotation baggage from MBAO from entering the merged import in the first place.

What I checked:

- Read the imported issue context from `__issue_context__.json`.
- Inspected `src/ontology/cl-odk.yaml` and confirmed CL uses base merging.
- Inspected the generated import rule in `src/ontology/Makefile` and confirmed `annotation_properties` is already intended as a keep-list.
- Verified that the current `src/ontology/imports/merged_import.owl` contains `skos:prefLabel` assertions on CL terms, which matches the reported symptom.
- Applied the config change only in `src/ontology/cl-odk.yaml`.

Validation:

- Reviewed the resulting git diff for `src/ontology/cl-odk.yaml`.
- I did not regenerate `imports/merged_import.owl` locally, because the requested fix is in the ODK source configuration and regenerating imports would produce additional generated-file churn outside the minimal source change.

Completed checklist:

- [x] Identify where MBAO import behavior is configured.
- [x] Confirm the current merged import contains the problematic `skos:prefLabel` assertions.
- [x] Make the minimal source-level configuration change.
- [x] Commit only the edited source file.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v2:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25587949052)
