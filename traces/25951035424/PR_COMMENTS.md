## Summary

Added a new CL term for `hybrid osteochondral skeletal cell` as `CL_9900001`.

## Rationale

The issue requested a new term for a murine rib periosteal/callus skeletal cell with hybrid osteochondral properties. I did not find an existing exact label for the requested term in `src/ontology/cl-edit.owl`. The requested parent label `skeletal cell` was not present as an exact CL label, so I used the existing parent `skeletogenic cell` (`CL_0007001`), which covers cells with potential to form skeletal cell types.

The new term includes:

- PMID-supported textual definition using `PMID:30983567`.
- Issue tracker annotation pointing to issue #3534.
- Contributor ORCID from the request.
- `terms:creator "GitHub Copilot"` and a `terms:date` timestamp.
- `SubClassOf` parentage to `skeletogenic cell`.
- `part_of` periosteum (`UBERON_0001434`) to reflect the requested anatomical location.
- Mouse taxon restriction (`NCBITaxon_10090`) because the issue and supporting publication describe the cell in mice.

## Completed Checklist

- Checked for an existing exact requested term label in `cl-edit.owl`.
- Checked the proposed parent and used the closest existing CL parent.
- Checked local usage of periosteum and taxon restriction axioms.
- Reviewed PMID:30983567 via the available publication record/full text source after `aurelian` was unavailable locally.
- Reviewed the final ontology diff.
- Ran `git diff --check` successfully.

`robot` was not installed in this environment, so I could not run `robot convert` or `robot reason`.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951035424)
