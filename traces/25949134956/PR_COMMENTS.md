Added a new Cell Ontology term for `fibrochondrocyte progenitor cell` as `CL_9900000` in `src/ontology/cl-edit.owl` to address issue #3458.

The new stanza includes:

- A textual definition supported by `PMID:31871141` and `PMID:36338137`.
- The related synonym `FCP` annotated as an abbreviation from `PMID:31871141`.
- `term_tracker_item` metadata linking back to issue `#3458`.
- Dublin Core contributor, creator, and timestamp metadata, including the requested ORCID.
- Parentage under `mesenchymal cell` (`CL_0008019`) and `progenitor cell` (`CL_0011026`).
- A `part_of some fibrocartilage` restriction using `UBERON_0001995`.
- Marker-based expression axioms for `COL1A1` and `COL3A1`, which are the fibrochondrocyte markers already represented in the local ontology file.

Rationale:

- The cited meniscus single-cell study (`PMID:31871141`) explicitly identifies fibrochondrocyte progenitors (`FCP`) as a progenitor population at the root of developmental trajectories, marked by `COL1A1`, `COL3A1`, `MCAM/CD146`, and `MYLK`, with colony-forming and osteogenic/adipogenic differentiation potential.
- The additional meniscus progenitor study (`PMID:36338137`) supports the broader progenitor characterization and regenerative context for human meniscal fibrocartilage.
- I kept the logical axioms conservative. The issue notes that a separate follow-up can add the reciprocal lineage link from `fibrochondrocyte`, so I did not add a stronger lineage axiom here.
- I did not add explicit axioms for `MCAM/CD146` and `MYLK` because I could not verify their PR identifiers from the local ontology file, and the project guidance says not to guess ontology IDs.

Completed checks:

- Reviewed the imported issue context from `__issue_context__.json`.
- Confirmed the key biology from the cited publications, using the accessible PMC full text for `PMID:31871141` and the PubMed abstract/search result for `PMID:36338137`.
- Checked existing CL usage in `src/ontology/cl-edit.owl` for the parent terms, fibrocartilage location, existing fibrochondrocyte term, and current metadata patterns.
- Reviewed the final diff before commit.

Validation:

- A targeted manual syntax review of the inserted stanza was completed.
- `robot` validation could not be run in this environment because the `robot` command is not installed.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25949134956)
