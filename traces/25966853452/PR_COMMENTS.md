This change implements the morphology pattern requested in issue #3536 in the local ontology edit file and relations documentation.

In `src/ontology/cl-edit.owl`, I added a new NTR term, `CL_9903536 cuboidal epithelial cell`, with a logical definition as `epithelial cell` and `has_characteristic some cuboid (PATO:0001872)`, placed under the existing `columnar/cuboidal epithelial cell`. I also added an equivalent class axiom for `squamous epithelial cell` using `has_characteristic some flattened (PATO:0002254)`.

I then applied the pattern to explicit downstream terms. For epithelial classes that are clearly cuboidal by label or definition, I re-used the new cuboidal parent where appropriate, including `stratified cuboidal epithelial cell`, `lens epithelial cell`, `luminal cell of lactiferous duct`, `renal principal cell`, `renal intercalated cell`, `pulmonary alveolar type 2 cell`, and `intercalated cell of salivary gland`. For additional cell types whose definitions explicitly state squamous or cuboidal morphology, I added direct `has_characteristic` axioms, including `Claudius cell`, `corneocyte`, `squamous cell of epidermis`, `keratinized squamous cell of esophagus`, `squamous endothelial cell`, `squamous granulosa cell`, and `cuboidal granulosa cell`.

In `docs/relations_guide.md`, I extended the morphology section to show the intended epithelial-shape usage of `has characteristic`, with `flattened` for squamous epithelial cells and `cuboid` for cuboidal epithelial cells. I also noted that the PATO label is `cuboid` while `cuboidal` is an exact synonym, to make the modeling choice explicit.

Completed checklist:
- confirmed the issue context locally from `__issue_context__.json`
- confirmed the morphology terms against the current PATO release: `PATO:0002254` = `flattened`, `PATO:0001872` = `cuboid` (exact synonym `cuboidal`)
- checked existing CL usage for `has characteristic` and the relevant epithelial terms before editing
- added the new NTR term with issue link and timestamp metadata
- updated the relations guide to document the pattern
- validated syntax with `robot convert -i src/ontology/cl-edit.owl -o /tmp/cl-edit-3536.owl`
- validated reasoning with `robot reason --reasoner ELK -i src/ontology/cl-edit.owl -o /tmp/cl-reasoned-3536.owl`

Scope note:
- I did not edit `src/patterns/dosdp-patterns/*`. The local repository instructions for this task explicitly limited edits to `src/ontology/cl-edit.owl` and files under `docs/`, so I kept the implementation within those allowed locations.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25966853452)
