## Summary

Addresses obophenotype/cell-ontology#3559 by adding three commonly used abbreviations as exact synonyms on the requested CL terms.

| Term | ID | Abbreviation added |
| --- | --- | --- |
| peripheral blood mononuclear cell | CL:2000001 | PBMC |
| leukocyte | CL:0000738 | WBC |
| retinal pigment epithelial cell | CL:0002586 | RPE |

## Rationale

The requester reported that LLM-based extraction of cell-type strings from experimental sample metadata frequently produces abbreviations like `PBMC`, `WBC`, and `RPE`, but these abbreviations are not currently registered as synonyms in CL, so their text-to-ontology mapping fails.

In the issue thread:

- @addiehl (MEMBER) confirmed that `PBMC` and `WBC` are safe to add as exact synonyms.
- @scheuerm (COLLABORATOR) confirmed that `RPE` is appropriate as a synonym for retinal pigment epithelial cell.

## Synonym encoding

Each abbreviation is added as an `oboInOwl:hasExactSynonym` with synonym type `OMO:0003000` (abbreviation), consistent with the existing CL convention for abbreviation synonyms (e.g. `D2-MSN` on CL:4023029, `COP` on CL:4023059, `GBC` on CL:4023164). Example axiom:

```
AnnotationAssertion(Annotation(oboInOwl:hasSynonymType obo:OMO_0003000) oboInOwl:hasExactSynonym obo:CL_2000001 "PBMC")
```

## Checklist

- [x] Confirmed the three target term IDs and labels by grepping `src/ontology/cl-edit.owl`.
- [x] Verified none of the three abbreviations (`PBMC`, `WBC`, `RPE`) were already present in the edit file.
- [x] Confirmed that "white blood cell" was already an exact synonym on CL:0000738 (the new `WBC` entry is the corresponding abbreviation, not a duplicate).
- [x] Used the existing CL pattern for abbreviation synonyms (`hasExactSynonym` annotated with `hasSynonymType OMO:0003000`).
- [x] Followed CLAUDE.md guidance: did not add `terms:date` / `terms:creator` to existing terms.
- [x] Committed on branch `issue-3559` and signed the commit message as `GitHub Copilot`.

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25980940449)
