This change addresses issue `#3243` by updating `CL_0000135` to the circulating fibrocyte concept supported by the issue literature, rather than the older inactive-fibroblast wording that was previously attached to the term.

Changes made:

- Renamed `CL_0000135` label from `fibrocyte` to `circulating fibrocyte`.
- Replaced the old text definition with a literature-backed definition describing a hematopoietic-origin stromal cell circulating in peripheral blood, coexpressing CD45/CD34/collagen I, and contributing to antigen presentation, angiogenesis, wound repair, and fibrosis.
- Added `fibrocyte` as a broad synonym and `monocyte-derived fibrocyte` as a narrow synonym.
- Replaced the uncited marker-heavy comment with a shorter comment restricted to literature-supported features.
- Changed the asserted lineage from `develops from` some fibroblast to `develops from` some monocyte, which is better aligned with the cited fibrocyte literature.

Consistency fixes made because of the rename:

- `CL_1000308` (`fibrocyte of adventitia of ureter`) was moved off `CL_0000135` and back under `CL_0000057` (`fibroblast`), since it is an anatomical/tissue fibrocyte term and should not become a subclass of a circulating blood cell.
- `CL_1000693` (`kidney interstitial fibrocyte`) was also moved from `CL_0000135` to `CL_0000057` for the same reason.
- A stale inferred parent axiom making `CL_0000388` (`tendon cell`) a subclass of `CL_0000135` was corrected to keep it under `fibroblast`.

Literature consulted:

- `PMID:9177213`
- `PMID:20305780`
- `doi:10.1186/1755-1536-5-S1-S6`
- I also reviewed issue-cited review material around fibrocyte biology and wound repair to keep the revised comment conservative.

Validation completed:

- Checked affected axioms directly in `src/ontology/cl-edit.owl`.
- Ran `robot convert -i src/ontology/cl-edit.owl -o /tmp/cl-edit.owl.ofn` successfully to confirm parser-level validity.

Commit created locally:

- `4242ad3` `Address issue #3243 by redefining circulating fibrocyte`

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25610257361)
