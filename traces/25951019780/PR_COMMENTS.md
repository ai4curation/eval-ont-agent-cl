## Summary

Added two requested stem cell memory T cell subset terms to `src/ontology/cl-edit.owl`:

- `CL_0020033` stem cell memory CD4-positive, alpha-beta T cell
- `CL_0020034` stem cell memory CD8-positive, alpha-beta T cell

Each term includes a textual definition with PMID xrefs, exact synonyms with references, ORCID contributors from the issue, `terms:creator`, `terms:date`, and `term_tracker_item` links back to issue #3452. The CD4 term is asserted under `CL_0000897` and the CD8 term is asserted under `CL_0000909`, matching the requested parents.

## Rationale

OLS currently returns exact matches for these requested labels as `CL_0020033` and `CL_0020034`, while this local ontology copy did not contain those terms. I used the OLS IDs rather than minting duplicate `CL_99xxxxx` identifiers.

The broader comment about species-specific memory T cell and naive T cell modeling was left out of scope for this NTR.

## Checklist

- Checked local `cl-edit.owl` for existing TSCM/stem cell memory terms.
- Checked the proposed parent terms `CL_0000897` and `CL_0000909` in `cl-edit.owl`.
- Checked DOSDP patterns for an existing memory T cell pattern; none was found.
- Checked OLS for the requested labels and TSCM synonyms.
- Checked PMID context through PubMed/PMC web pages because `aurelian` is not installed in this environment.
- Ran `git diff --check` successfully.

## Validation

`robot convert` could not be run because `robot` is not installed in this environment. `owltools` is also not installed. Lightweight checks confirmed the inserted declarations, labels, definitions, synonyms, contributors, issue links, and parent axioms are present, and `git diff --check` passed.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951019780)
