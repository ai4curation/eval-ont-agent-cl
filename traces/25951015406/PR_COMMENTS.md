## Summary

Removed redundant `rdfs:label` annotation assertions for imported annotation properties from `src/ontology/cl-edit.owl`.

The removed labels covered imported annotation properties from IAO, OBO in OWL, UBERON synonym type properties, and `rdfs:seeAlso`. Local CL-owned subset annotation-property labels were left intact.

## Rationale

These imported annotation properties already receive labels through imports, so keeping duplicate labels in the edit file can produce avoidable ontology diffs. The change keeps the edit file focused on CL-owned content and local metadata.

## Checklist

- Read `__issue_context__.json`.
- Confirmed the relevant redundant label assertions were in `src/ontology/cl-edit.owl`.
- Removed only redundant imported annotation-property `rdfs:label` assertions.
- Rechecked that no `rdfs:label` assertions remain for `oboInOwl:*`, `obo:IAO_*`, `uberon:*`, or `rdfs:seeAlso` annotation properties.
- Committed the ontology change locally on branch `issue-3333`.

## Validation

Attempted to run:

```bash
robot convert -i src/ontology/cl-edit.owl -o /tmp/cl-edit-check.owl
```

This could not be completed because `robot` is not installed in the local environment.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951015406)
