# Improve definition of Islands of Calleja granule cell (CL:4030053)

Addresses obophenotype/cell-ontology#3447.

## Summary of changes

Single term edited: **CL:4030053**.

### Label
- Renamed: `Island of Calleja granule cell` → `Islands of Calleja granule cell` (plural "Islands", matching the standard anatomical terminology and the curator's request).
- Added the previous label as an `oboInOwl:hasExactSynonym` so that existing references and searches against the old form continue to resolve.

### Textual definition
Replaced the prior short definition

> "A DRD1-expressing, medium spiny neuron-like granule cell that is part of an Island of Calleja."

with the curator-provided revised definition describing:
- the GABAergic nature of the cell
- D1 and D3 dopamine receptor expression (Drd1; Drd3)
- GABAergic markers (GAD1/2)
- densely packed granule cell clusters in the olfactory tubercle within the ventral striatum
- dense dopaminergic input from the ventral tegmental area (VTA)
- functional associations with self-grooming and depression-like behaviors

### Definition xrefs
Per the issue ("Include the references cited but do not replace the existing ones"), the definition now carries three xrefs:
- `PMID:34795450` (Zhang et al., 2021 — newly added)
- `PMID:37898623` (Zhang et al., 2023 — newly added)
- `doi:10.1016/j.cub.2021.10.015` (existing — preserved)

### Logical axioms
- Added `SubClassOf(CL_4030053 CL_0000617)` to assert the term is a GABAergic neuron, as requested in the issue ("include axiom 'Gabaergic neuron'").
- The existing parent `SubClassOf CL_0000120` (granule cell), the `has_soma_location` axiom for the Islands of Calleja within the olfactory tubercle, and the DRD1 expression axiom (`RO_0002292 PR_000001175`) were all kept intact.

### Metadata
- Updated the `rdfs:comment` to reflect the new label ("Islands of Calleja…").
- Added `IAO_0000233` (term_tracker_item) pointing to issue #3447.
- Did not modify `terms:date` (this is an edit, not a new term).
- Did not add myself as `dc:creator` (per editor guidelines, creators are only added when terms are first created).

## Editorial decisions worth flagging
- The curator-supplied text contained inline parenthetical citations of the form "(Zhang et al., 2021; Zhang et al., 2023)". I removed those from the definition body since the corresponding PMIDs are now formally attached as definition xrefs — this is the standard OBO convention and avoids redundancy. Happy to put them back inline if reviewers prefer.
- Capitalised "Islands of Calleja" within the definition (the curator wrote it lower-case in the issue body).
- Expanded "VTA" to "ventral tegmental area (VTA)" on first use.
- Minor grammar fix: "functionally associated with" → "is functionally associated with".

## Checklist
- [x] Read issue #3447 and the linked context comment (#3321).
- [x] Located CL:4030053 in `src/ontology/cl-edit.owl` and read its existing axioms.
- [x] Verified `CL_0000617` is the canonical term for "GABAergic neuron".
- [x] Verified `UBERON_0001881` (Islands of Calleja) and `UBERON_0005403` (olfactory tubercle) are the locations already referenced on the term.
- [x] Verified `CL_0000120` (granule cell) is the current named parent.
- [x] Preserved the existing `doi:10.1016/j.cub.2021.10.015` reference.
- [x] Added new PMIDs (`PMID:34795450`, `PMID:37898623`) as definition xrefs.
- [x] Added the GABAergic-neuron parent class assertion.
- [x] Kept all previously asserted SubClassOf axioms.
- [x] Added the old label as an exact synonym (lossless rename).
- [x] Added a `term_tracker_item` link to issue #3447.
- [x] Confirmed only `src/ontology/cl-edit.owl` was modified.

## Validation
`robot` was not available in this environment, so I could not run `robot reason` or `robot convert -vvv`. The edit only adds well-formed annotation assertions and a SubClassOf axiom against terms that already exist in the ontology, mirroring patterns used elsewhere in `cl-edit.owl`, so no syntactic issues are expected. CI/ODK validation on the PR will provide authoritative confirmation.

Signed-off-by: GitHub Copilot

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951220632)
