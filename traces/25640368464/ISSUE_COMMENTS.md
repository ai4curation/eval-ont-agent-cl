# Issue #3224 Resolution

## Status: Fixed

Changes have been committed to resolve the issue where Protégé modifications occurred in `merged_import.owl` instead of `cl-edit.owl`.

### Solution
Added `skos:prefLabel` to the global annotation properties filter in the import configuration. This prevents `skos:prefLabel` from being imported into `merged_import.owl`, eliminating the duplicate label annotations that were interfering with Protégé's editing workflow.

### Files Modified
- `src/ontology/Makefile` - Added `skos:prefLabel` to ANNOTATION_PROPERTIES variable
- `src/ontology/cl-odk.yaml` - Added `skos:prefLabel` to the annotation_properties list under import_group

The fix follows the suggestion in the issue to exclude `skos:prefLabel` from MBAO and other import sources, since these values are typically identical to the standard `rdfs:label` values already imported.

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-haiku-4-5-20251001`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25640368464)
