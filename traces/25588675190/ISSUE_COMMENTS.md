I've implemented the suggested fix by modifying the ODK configuration to exclude `skos:prefLabel` from MBAO imports.

The change adds `annotation_property_exclusions` to the MBAO import configuration in `cl-odk.yaml`, which should prevent the duplicative `skos:prefLabel` annotations from being imported into `merged_import.owl`. This will resolve the issue where Protégé was modifying `merged_import.owl` instead of `cl-edit.owl` when attempting to obsolete terms.

The import configuration will need to be regenerated for this change to take effect in the next build.

Changes committed in PR #<NN>

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-sonnet-4-5-20250929`
- Agent config: `ai4curation/cl-agent-config@v2:.`
- Iteration: `2`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25588675190)
