This change implements the ontology and documentation parts of issue #3536.

What changed:
- Added new term `CL_9900001` `cuboidal epithelial cell` with text definition, issue link, creator/date metadata, and logical definition:
  `epithelial cell` and `has characteristic` some `PATO:0001872` (`cuboid`).
- Added a text definition and logical definition to `CL_0000076` `squamous epithelial cell`:
  `epithelial cell` and `has characteristic` some `PATO:0002254` (`flattened`).
- Added explicit `has characteristic` shape axioms to affected squamous/cuboidal classes that were already named or defined that way, including stratified epithelial subclasses, alveolar/endothelial/granulosa examples, kidney collecting duct cells, lens epithelial cell, lactiferous duct luminal cell, and salivary intercalated duct cell.
- Updated `docs/relations_guide.md` to document the epithelial morphology pattern using `has characteristic` with PATO shape terms.

Rationale:
- The issue asks for a dedicated cuboidal epithelial term and for squamous/cuboidal morphology to be captured with `has characteristic`.
- Defining `squamous epithelial cell` and `cuboidal epithelial cell` from `epithelial cell` plus a PATO morphology quality gives a reusable genus-differentia pattern.
- Adding direct shape axioms to already named/defined squamous or cuboidal descendants makes the morphology explicit rather than relying only on textual definitions.
- I deliberately avoided adding shape axioms to variable-form classes whose definitions say they can be squamous/cuboidal/columnar depending on state or context.

Scope note:
- I did not add DOSDP files under `src/patterns`. The task environment instructions for this run limited ontology edits to `src/ontology/cl-edit.owl` and `docs/`.

Completed checklist:
- [x] Read the imported issue context
- [x] Reviewed existing squamous/cuboidal terms in `src/ontology/cl-edit.owl`
- [x] Resolved the PATO identifier for cuboidal shape (`PATO:0001872`)
- [x] Added the new cuboidal epithelial term
- [x] Added the requested logical definition for squamous epithelial cell
- [x] Added explicit morphology axioms to affected classes
- [x] Updated the relations guide
- [x] Validated syntax and whitespace

Validation:
- `robot convert -i src/ontology/cl-edit.owl -o /tmp/cl-edit-3536.owl`
- `git diff --check -- src/ontology/cl-edit.owl docs/relations_guide.md`

Background/reference notes:
- Used the existing local issue context plus the ontology’s nearby epithelial references.
- Verified `flattened` as `PATO:0002254`.
- Verified the cuboidal quality term as `PATO:0001872` (`cuboid`, with exact synonym `cuboidal`) from the public PATO source.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25976289357)
