This change addresses issue #3224 by removing MBAO `skos:prefLabel` annotations during merged import generation.

Rationale:
- The issue report identified imported MBAO `skos:prefLabel` axioms as the trigger for Protégé writing obsoletion edits into `merged_import.owl` instead of `cl-edit.owl`.
- CL already customizes ODK behavior in `src/ontology/cl.Makefile`, so the least invasive fix was to override the `imports/merged_import.owl` rule there and add one targeted SPARQL update.
- The SPARQL update deletes only `skos:prefLabel` assertions on MBAO IRIs, leaving other imported annotation content unchanged.

Files changed:
- `src/ontology/cl.Makefile`
- `src/sparql/remove-mbao-skos-prefLabel.ru`

Completed checklist:
- [x] Read the imported issue context from `__issue_context__.json`
- [x] Confirmed that MBAO `skos:prefLabel` annotations are present in the tracked `src/ontology/imports/merged_import.owl`
- [x] Implemented a targeted import-pipeline fix without editing generated ODK files
- [x] Ran a local rebuild of `imports/merged_import.owl` with `make imports/merged_import.owl PAT=false -B`
- [x] Verified that the rebuilt merged import no longer contains MBAO `skos:prefLabel` axioms
- [x] Committed only the source changes required for the fix

Validation notes:
- `sh run.sh make ...` was not usable in this environment because `docker` is unavailable.
- I therefore ran the equivalent local build directly with `make` and the installed `robot` tool.
- I did not commit the regenerated `src/ontology/imports/merged_import.owl` because the rebuild refreshed mirrors and introduced broad unrelated generated diffs. The committed change is the source-level fix that produces the expected result on rebuild.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v2:.`
- Iteration: `3`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25588721747)
