## Summary

Updated the two requested human dendritic cell terms:

- CL_0001057 (myeloid dendritic cell, human)
- CL_0001058 (plasmacytoid dendritic cell, human)

For each term, I added CD14-negative to the textual definition and added the corresponding logical restriction using the existing CL marker pattern:

- `ObjectSomeValuesFrom(obo:CL_4030046 obo:PR_000001889)`

where `CL_4030046` is `lacks_plasma_membrane_part` and `PR_000001889` is the CD14 protein identifier already used in CL marker axioms.

## Rationale

The issue requested adding lack of CD14 expression to CL_0001057 and CL_0001058, with additional OMIP references supporting human flow cytometry gating strategies. I preserved the existing `PMID:22343568` definition xref and added the requested OMIP PMIDs as additional definition xrefs:

- PMID:34260151
- PMID:36470845
- PMID:37254600
- PMID:38343094
- PMID:40095400

I did not apply this change to CL_0001056 (dendritic cell, human), because the issue thread explicitly notes human dendritic cell subsets that can express CD14.

## Checklist

- Read imported issue title, body, and comments from `__issue_context__.json`.
- Checked the current axioms for CL_0001057 and CL_0001058 in `src/ontology/cl-edit.owl`.
- Checked the existing CL pattern for absent plasma membrane markers.
- Confirmed CD14 is represented by `PR_000001889` in existing CL marker axioms.
- Attempted to use `aurelian fulltext` for cited PMIDs, but `aurelian` was not installed in this environment.
- Verified PubMed metadata for the requested PMIDs via NCBI E-utilities.
- Ran `git diff --check` on the ontology edit.
- Confirmed both requested terms now have a CD14-negative logical restriction.

## Validation

`git diff --check -- src/ontology/cl-edit.owl` passed.

Full ROBOT validation was not run because `robot` is not available on the PATH in this environment.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951031196)
