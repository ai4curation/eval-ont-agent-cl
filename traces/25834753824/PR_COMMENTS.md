# Improve definition and label for CL:4030053 (Islands of Calleja granule cell)

Addresses obophenotype/cell-ontology#3447.

## Changes to `src/ontology/cl-edit.owl`

### Label
- Renamed `Island of Calleja granule cell` → `Islands of Calleja granule cell` (plural form matches the anatomical structure name and the wording used throughout the referenced literature).
- The prior label has been preserved as an `oboInOwl:hasExactSynonym` so that downstream tools that previously resolved the singular form continue to work.

### Textual definition
Replaced the previous one-sentence definition with a richer description that mirrors the wording requested in the issue:

> A GABAergic neuron that resides in the Islands of Calleja and shows the cytoarchitectural and molecular features characteristic of this granule-like cell population. In mice and primates, it expresses D1 and D3 dopamine receptors (Drd1; Drd3) and GABAergic markers (GAD1/2), and forms densely packed granule cell clusters in the olfactory tubercle within the ventral striatum. It receives dense dopaminergic input from the ventral tegmental area (VTA) and has been functionally associated with self-grooming behaviors and depression-like behaviors.

The original definition reference (`doi:10.1016/j.cub.2021.10.015`) is preserved as instructed in the issue ("do not replace the existing ones") and the two newly cited PMIDs were added as additional definition xrefs:
- `PMID:34795450` (Zhang et al., 2021)
- `PMID:37898623` (Zhang et al., 2023)

### Parents / logical axioms
- Added `SubClassOf CL:0000617` (GABAergic neuron) per the explicit request in the issue ("include axiom 'Gabaergic neuron'").
- Kept the existing `SubClassOf CL:0000120` (granule cell) — these neurons are still granule-like in cytoarchitecture, so both parents apply.
- Kept the existing location axiom (`has part_of` Islands of Calleja in the ventral striatum) and the `expresses` axiom for the DRD1 protein.

### Metadata
- Added `IAO:0000233` (`term_tracker_item`) pointing to https://github.com/obophenotype/cell-ontology/issues/3447, per the editors' guide in `CLAUDE.md`.
- The `terms:date` was left at its original value because this is an edit to an existing term rather than a new term.
- No `terms:creator` was added: per `CLAUDE.md`, creator should only be added when creating a new term, not when editing.
- Updated the species-specific `rdfs:comment` to use the corrected "Islands" form for internal consistency.

## Checklist
- [x] Label corrected to plural form and old label retained as exact synonym
- [x] Definition updated with the wording from the issue
- [x] New PMIDs added as definition xrefs alongside the existing doi
- [x] `SubClassOf CL:0000617` (GABAergic neuron) added
- [x] `term_tracker_item` axiom added linking back to issue #3447
- [x] Existing `expresses DRD1` and location axioms preserved
- [x] No `terms:creator` added (per CLAUDE.md rule for edits to existing terms)
- [x] Comment updated for internal label consistency

## Validation notes
- ROBOT was not available in this evaluation environment, so I was unable to run `robot reason` for logical validation. The edit is syntactically a strict superset of the prior valid axiom block (label changed, one synonym added, one tracker annotation added, one SubClassOf added, comment text edited) and uses only properties already in use elsewhere in `cl-edit.owl`, so I do not expect parser or reasoner regressions. A maintainer should re-run the standard ODK validation pipeline on merge.
- `aurelian` was not available, so I could not pull full text for the two new PMIDs. I trusted the issue author's identification of authors/years and used the references exactly as cited. If the curators have other preferred citation styling (e.g., adding the doi for these PMIDs as well) that's an easy follow-up edit.

Signed: GitHub Copilot

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25834753824)
