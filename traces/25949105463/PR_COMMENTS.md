Added `CL_0020022` `prehypertrophic chondrocyte` to `src/ontology/cl-edit.owl` to address issue #3460.

Rationale:
- The issue requested a new term for prehypertrophic chondrocyte with synonym `preHTC`, references, and contributor metadata.
- OLS lookup showed that this term already exists upstream in CL as `CL:0020022`, but it was absent from this local checkout, so I backported the upstream identifier and core axiom set rather than minting a new `CL_99xxxxx` term.

Changes made:
- Added `Declaration(Class(obo:CL_0020022))`.
- Added the label `prehypertrophic chondrocyte`.
- Added the requested textual definition with PMID references: `29985449`, `31871141`, and `34137454`.
- Added the related synonym `preHTC` as an abbreviation, with PMID support from `31871141`.
- Added contributor metadata for ORCID `0009-0000-8480-9277`.
- Added `terms:creator "GitHub Copilot"` and a `terms:date` timestamp for the new term.
- Added a tracker link back to issue `#3460` via `IAO_0000233`.
- Added the asserted parent `chondrocyte (CL_0000138)` and the developmental restriction `directly develops from some hypertrophic chondrocyte (RO_0002207 some CL_0000743)` to match the released upstream `cl.owl` term as published.

Background research / checks:
- Read `__issue_context__.json` for the imported issue title/body/comments.
- Checked the local ontology file for existing `chondrocyte`, `hypertrophic chondrocyte`, and any existing `prehypertrophic chondrocyte` term.
- Queried OLS to confirm the requested label already exists upstream as `CL:0020022` and inspected the released `cl.owl` serialization to mirror the upstream axiom pattern.
- Retrieved primary publication metadata/abstracts from NCBI E-utilities for PMIDs `29985449`, `31871141`, and `34137454`.

Validation:
- Attempted a parse check with `robot convert`, but `robot` is not installed in this workspace.
- Because of that, no local ROBOT syntax/reasoner validation was run here.

Checklist completed:
- [x] Read imported issue context.
- [x] Confirmed the term was missing from the local ontology file.
- [x] Confirmed the canonical upstream CL identifier in OLS.
- [x] Added the term only in `src/ontology/cl-edit.owl`.
- [x] Committed only the ontology edit.
- [x] Left `ISSUE_COMMENTS.md` and `PR_COMMENTS.md` uncommitted.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25949105463)
