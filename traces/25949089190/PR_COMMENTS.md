Removed redundant `rdfs:label` assertions for imported annotation properties from `src/ontology/cl-edit.owl`.

Rationale:
- These labels are already provided by imported modules, so keeping local duplicates only adds noise and creates avoidable diffs.
- The edit was limited to imported annotation properties and did not touch locally maintained CL subset properties.

What changed:
- Removed redundant labels on imported annotation properties in the `obo:IAO_0000028`, `oboInOwl:*`, `uberon:*`, and `rdfs:seeAlso` blocks.

Checks completed:
- Confirmed the imported issue context and scoped the request to redundant imported-property relabeling.
- Searched `src/ontology/cl-edit.owl` for `AnnotationAssertion(rdfs:label ...)` axioms on imported annotation properties.
- Reviewed the resulting diff to confirm the change was limited to those labels.
- Ran `git diff --check` to verify there were no whitespace issues.

Validation notes:
- `robot` is not available in this environment, so I could not run `robot convert` or reasoning-based validation.

Commit:
- `42c9baf` `Remove redundant labels from imported annotation properties`

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25949089190)
