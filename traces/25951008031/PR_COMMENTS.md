## Summary

Addressed issue #3500 by adding explicit mouse taxon constraints to:

- DN2a thymocyte (CL_0002423)
- DN2b thymocyte (CL_0002424)

Each term now has a `SubClassOf` axiom for `RO_0002162 some NCBITaxon_10090` (`in taxon: Mus musculus`), annotated with the originating issue URL as `IAO_0000233` provenance.

## Rationale

The existing CL comments for both terms already stated that the terms are observed in mice and warned that double-negative thymocyte markers differ across species, especially mouse versus human thymus. The issue requested making that species restriction machine-readable. Adding explicit `in taxon` subclass axioms captures the intended mouse restriction without changing the existing labels, definitions, comments, or logical marker definitions.

I also checked the parent DN2 thymocyte term (CL_0000806) and left it unchanged, since it is broader and species-neutral in the current ontology, while DN2a/DN2b are the requested mouse Kit-high/Kit-low subdivisions.

## Checklist

- [x] Read imported issue context from `__issue_context__.json`.
- [x] Checked existing axioms for CL_0002423 and CL_0002424 in `src/ontology/cl-edit.owl`.
- [x] Checked existing use of `RO_0002162` / `NCBITaxon_10090` taxon constraints for consistency.
- [x] Checked the parent DN2 thymocyte term for scope.
- [x] Tried to fetch cited PMIDs with `aurelian fulltext`; `aurelian` was not installed in this environment.
- [x] Used publication pages as fallback background for the cited literature.
- [x] Added only the requested ontology axioms.
- [x] Ran `git diff --check`.
- [x] Confirmed the new taxon axioms are present for both terms.

## Validation

`robot convert` could not be run because `robot` is not installed in this environment. Syntax-level checks performed locally were limited to `git diff --check` and targeted verification of the inserted functional-syntax axioms.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951008031)
