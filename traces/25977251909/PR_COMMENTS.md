## Summary

Adds three abbreviation synonyms requested in obophenotype/cell-ontology#3559, to help downstream LLM-based ontology mapping tools recognise commonly used cell-type abbreviations from experimental sample metadata.

## Changes

In `src/ontology/cl-edit.owl`, added the following exact synonyms with synonym type `OMO:0003000` (abbreviation):

| Term | ID | Synonym added | Scope |
|---|---|---|---|
| peripheral blood mononuclear cell | CL:2000001 | `PBMC` | exact (abbreviation) |
| leukocyte | CL:0000738 | `WBC` | exact (abbreviation) |
| retinal pigment epithelial cell | CL:0002586 | `RPE` | exact (abbreviation) |

Example axiom shape used (per existing convention in the file):

```
AnnotationAssertion(Annotation(oboInOwl:hasSynonymType obo:OMO_0003000) oboInOwl:hasExactSynonym obo:CL_2000001 "PBMC")
```

## Rationale & decisions

- **Synonym scope.** Alex Diehl (@addiehl, MEMBER) suggested `PBMC` and `WBC` could safely be added as exact synonyms; @scheuerm confirmed `RPE` was appropriate as a synonym for retinal pigment epithelial cell. Each abbreviation is in 1:1 correspondence with its label term, so `exact` (rather than `related`) is appropriate. This is also consistent with many existing CL entries — e.g. `CL:4023029` ("D2-MSN", "iSPN"), `CL:4023059` ("COP"), `CL:4023164` ("GBC"), `CL:4030032` ("VIC") — where abbreviations are recorded as `hasExactSynonym` with the `OMO_0003000` synonym type.
- **Synonym type.** The `OMO_0003000` (abbreviation) annotation distinguishes these from full-word synonyms (`leucocyte`, `white blood cell`), preserving useful metadata for downstream consumers.
- **No other changes.** The terms' definitions, logical axioms, and other annotations are untouched; this is a synonym-only addition. No new IDs were minted.

## Validation checklist

- [x] Searched `cl-edit.owl` for any pre-existing `PBMC` / `WBC` / `RPE` synonyms — none found, so no duplicates introduced.
- [x] Confirmed each target term ID resolves to the expected label:
  - `CL:2000001` → "peripheral blood mononuclear cell"
  - `CL:0000738` → "leukocyte"
  - `CL:0002586` → "retinal pigment epithelial cell"
- [x] Followed the existing in-file convention for abbreviation synonyms (`OMO_0003000` + `hasExactSynonym`).
- [x] Edited only `src/ontology/cl-edit.owl` per the project guide.
- [x] Single focused commit with a descriptive message, signed `GitHub Copilot`.

Note: `robot reason` was not available in this environment, so syntactic/logical validation should be re-run as part of CI before merge. The change is a pure annotation addition with no logical-axiom impact, so no reasoning changes are expected.

Closes #3559.

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25977251909)
