Implemented the requested taxon constraints for the two DN2 thymocyte subset terms discussed in issue #3500.

Changes made:
- Added `SubClassOf(... ObjectSomeValuesFrom(obo:RO_0002162 obo:NCBITaxon_10090))` to `CL_0002423` (DN2a thymocyte).
- Added `SubClassOf(... ObjectSomeValuesFrom(obo:RO_0002162 obo:NCBITaxon_10090))` to `CL_0002424` (DN2b thymocyte).
- Annotated both new taxon axioms with the issue URL using `IAO:0000233`, following existing curation patterns in `cl-edit.owl`.

Rationale:
- The issue body describes DN2a and DN2b as mouse-specific subdivisions based on Kit-high versus Kit-low expression in the classical DN1-DN4 staging framework.
- Both terms already carried comments stating they are observed in mice and warning that equivalent double-negative thymocyte marker schemes differ across species, especially between mouse and human.
- Adding explicit `in taxon` restrictions makes that species scope machine-readable and removes the ambiguity noted in the issue.

Validation and review performed:
- Inspected the existing term stanzas for `CL_0002423` and `CL_0002424` in `src/ontology/cl-edit.owl`.
- Checked nearby and global examples of existing `in taxon` assertions to match file style.
- Ran `robot convert --input src/ontology/cl-edit.owl --output /tmp/opencode/cl-edit-issue3500.owl` to confirm the edited ontology parses successfully.

Checklist completed:
- [x] Read imported issue context.
- [x] Confirmed the issue was specific enough to implement without clarification.
- [x] Added explicit mouse taxon constraints for both requested terms.
- [x] Validated ontology syntax with ROBOT.
- [x] Committed only the ontology file change locally.

Note:
- The environment did not provide the `aurelian` helper mentioned in repository guidance, so publication lookup was limited to the PMIDs and rationale already supplied in the issue context. The ontology change itself is narrow and consistent with the existing term comments.

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25977480233)
