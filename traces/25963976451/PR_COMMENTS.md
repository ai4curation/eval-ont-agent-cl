## Summary

Addresses obophenotype/cell-ontology#3163 by recording the CD14-negative phenotype on the two human dendritic cell terms identified in the issue:

- `CL_0001057` — myeloid dendritic cell, human
- `CL_0001058` — plasmacytoid dendritic cell, human

`CL_0001056` (dendritic cell, human) is intentionally **not** modified, in line with @Caroline-99's comment that some human DC subsets (e.g. dermal DCs) do express CD14, so the general human DC class should not assert CD14-negativity.

## Changes per term

For each of `CL_0001057` and `CL_0001058`:

1. **Logical definition** — added `lacks_plasma_membrane_part some CD14` to the existing `EquivalentClasses` axiom, using:
   - object property `CL:4030046` (`lacks_plasma_membrane_part`) — the relation already used for "CD14-negative monocyte"-style axioms in this ontology (e.g. `CL_0001054`) and recommended in `docs/relations_guide.md` for absence of a surface marker.
   - target `PR:000001889` (CD14 / monocyte differentiation antigen CD14) — the same PR term used as the CD14 marker in `CL_0001054` (CD14-positive monocyte) and in many other CL axioms.
2. **Textual definition** — appended "and CD14-negative" (myeloid) / "and CD14-negative" (pDC) so the text mirrors the logical definition, as required by the editor guidelines.
3. **Definition xrefs** — added the five OMIP references requested in the issue as additional `oboInOwl:hasDbXref` annotations on the `IAO:0000115` axiom, keeping the original `GOC:add` and `PMID:22343568` xrefs intact:
   - PMID:34260151 (OMIP-77)
   - PMID:36470845 (OMIP-87)
   - PMID:37254600 (OMIP-94)
   - PMID:38343094 (OMIP-101)
   - PMID:40095400 (OMIP-112)
4. **Issue tracker link** — added `IAO:0000233` (`term_tracker_item`) pointing to https://github.com/obophenotype/cell-ontology/issues/3163.

## Rationale

The OMIP panels cited in the issue (OMIP-77, -87, -94, -101, -112) all gate human myeloid and plasmacytoid DCs as CD14⁻ to separate them from monocytes. Asserting `lacks_plasma_membrane_part some CD14` on the two human-specific DC classes captures this standard gating in the logical definition while leaving the more general `CL_0001056` unchanged so it can continue to subsume CD14⁺ DC subsets such as `CL_0002526` (CD14-positive dermal dendritic cell).

`lacks_plasma_membrane_part` is the EL-safe shortcut for absence of a surface marker (per `docs/relations_guide.md`), and is the pattern already used in this ontology for other CD14-marker-based distinctions.

## Checklist

- [x] Read issue #3163 and its comments, including @Caroline-99's scope clarification.
- [x] Verified the existing axioms for `CL_0001057` and `CL_0001058` before editing.
- [x] Confirmed `CL_4030046` is `lacks_plasma_membrane_part` and `PR_000001889` is CD14 by cross-referencing existing CL axioms (e.g. `CL_0001054`).
- [x] Used the existing reference (`PMID:22343568`) and `GOC:add` xref alongside the five new OMIP PMIDs (per issue: "Add these references but do not replace existing references").
- [x] Updated textual definitions to mirror the logical definitions.
- [x] Did **not** modify `CL_0001056` per @Caroline-99's note.
- [x] Added `term_tracker_item` linking back to the issue on both terms.
- [x] Single targeted commit, signed "GitHub Copilot".

## Notes for reviewers

- Per CLAUDE.md, only existing terms were modified, so no `dc:date` / `dc:creator` annotations were added (those are reserved for new-term creation).
- No reasoning run was performed in this environment; please run `make test` / `robot reason` as part of normal CI to confirm classification is consistent.

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25963976451)
