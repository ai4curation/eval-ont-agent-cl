## Summary

Addresses issue #3559 by adding exact abbreviation synonyms to the requested Cell Ontology terms:

- `PBMC` as an exact synonym of `peripheral blood mononuclear cell` (`CL_2000001`)
- `WBC` as an exact synonym of `leukocyte` (`CL_0000738`)
- `RPE` as an exact synonym of `retinal pigment epithelial cell` (`CL_0002586`)

## Rationale

The issue requested these abbreviations to improve mapping of extracted sample metadata strings to CL terms. A maintainer comment indicated that `PBMC` and `WBC` can safely be added as exact synonyms, and a follow-up comment confirmed that `RPE` is appropriate as a synonym for retinal pigment epithelial cell.

## Checklist

- Checked the requested CL term IDs in `src/ontology/cl-edit.owl`.
- Checked that `PBMC`, `WBC`, and `RPE` were not already present as synonyms.
- Added synonym axioms only to `src/ontology/cl-edit.owl`.
- Committed the ontology change locally.

## Validation

- Ran `rg` checks to confirm the three new synonym assertions are present on the requested terms.
- Ran `git diff --check` successfully.
- `robot` validation was not run because `robot` is not installed in this environment.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951025641)
