## Summary

Addresses issue #3506 — revise `hypertrophic chondrocyte` (CL:0000743) to remove the outdated "terminally differentiated" framing, link it to Uberon, and add proper logical axioms.

## Changes

### `src/ontology/cl-edit.owl`

- **Replaced textual definition** (`IAO:0000115`) on CL:0000743 with the wording supplied in the issue:
  > "A chondrocyte that is part of the hypertrophic cartilage zone. This cell is significantly enlarged and characterised by high expression of type X collagen (COL10A1) in both humans and mice. It actively coordinates endochondral ossification by mineralising the extracellular matrix, attracting blood vessels via angiogenic signalling, and mediating the transition from cartilage to bone - often by transdifferentiating into an osteoblast rather than undergoing apoptosis."
  - Annotated with `oboInOwl:hasDbXref "PMID:25321476"` and `oboInOwl:hasDbXref "PMID:35179487"` (replacing the previous GO_REF:0000034 + PMID:15951842 xrefs, which supported the outdated wording).

- **Replaced `rdfs:comment`** with the explanatory text from the issue (PMID:35179487, PMID:41207902 cited inline). The previous comment ("is hypertrophic pathological or normal? and can it be described using a pato term?") was a stale editor note and was removed as requested ("REPLACE WHAT ALREADY EXISTS").

- **Added logical axioms** mirroring the new definition's genus-differentia structure (genus = chondrocyte, retained as the existing `SubClassOf obo:CL_0000138`):
  - `SubClassOf(obo:CL_0000743 ObjectSomeValuesFrom(obo:BFO_0000050 obo:UBERON_0008187))` — part_of some 'hypertrophic cartilage zone'
  - `SubClassOf(obo:CL_0000743 ObjectSomeValuesFrom(obo:RO_0002215 obo:GO_0001958))` — capable_of some 'endochondral ossification'

- **Added `term_tracker_item`** annotation (`IAO:0000233`) pointing to the issue URL.

### `src/ontology/imports/uberon_terms.txt`

- Added `UBERON:0008187` ('hypertrophic cartilage zone') so that the next refresh of `imports/merged_import.owl` will bring the term into the import module, per `docs/Adding_classes_from_another_ontology.md`. The import refresh itself (which needs Docker/ROBOT and >8GB RAM) should be run by a maintainer with the appropriate environment; it has not been executed in this PR.

## Research / validation

- **UBERON:0008187** confirmed via OLS as the IRI for "hypertrophic cartilage zone" (`http://purl.obolibrary.org/obo/UBERON_0008187`).
- **GO:0001958** for "endochondral ossification" is already present in `src/ontology/imports/merged_import.owl`, so the `capable_of` axiom resolves immediately.
- Searched for `present_in_taxon Homo sapiens` (NCBITaxon:9606 / RO:0002175) on CL:0000743 in `src/ontology/cl-edit.owl` — no such axiom exists, so nothing was removed for that point. Flagged in `ISSUE_COMMENTS.md` in case the requester was referring to an axiom in a component file outside the editable layer.
- I could not run `robot reason` in this environment (no `robot` / Docker available), so the changes have not been validated by the reasoner. Maintainers should re-run the standard QC pipeline before merge.

## Checklist (self)

- [x] Replace outdated text definition (with PMID xrefs)
- [x] Replace stale `rdfs:comment`
- [x] Add `part_of` axiom to UBERON:0008187
- [x] Add `capable_of` axiom to GO:0001958
- [x] Add `term_tracker_item` linking to issue #3506
- [x] Add UBERON:0008187 to `uberon_terms.txt`
- [x] Verify no `present_in_taxon` Homo sapiens axiom is present in `cl-edit.owl`
- [ ] Refresh `imports/merged_import.owl` (requires Docker/ROBOT — to be done by a maintainer)
- [ ] Validate with `robot reason` (could not run locally — see above)

Signed-off-by: GitHub Copilot

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25834928279)
