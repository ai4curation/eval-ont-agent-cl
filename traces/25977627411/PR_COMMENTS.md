Added a new subset annotation property, `cl:add_by_HRA`, to `src/ontology/cl-edit.owl` so CL can tag terms added by HRA and HuBMAP team members.

What changed:
- Declared `cl:add_by_HRA` as an annotation property.
- Marked it as a `oboInOwl:SubsetProperty`.
- Added a label `add_by_HRA`.
- Added a short comment describing its intended use.
- Added `terms:date` metadata.
- Added an issue-tracker link back to cell-ontology issue `#3590` using `obo:IAO_0000233`.

Rationale:
- The issue requests a new subset tag for tracking HRA-added terms.
- No term assignments were requested in the issue text, so the minimal consistent change is to introduce the subset property itself without altering any existing classes.

Checks completed:
- Read the imported issue context from `__issue_context__.json`.
- Confirmed the existing CL pattern for subset annotation properties in `src/ontology/cl-edit.owl`.
- Validated ontology syntax with `robot convert --input src/ontology/cl-edit.owl --output /tmp/opencode/cl-edit.ofn.owl`.
- Reviewed the final diff before committing.

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25977627411)
