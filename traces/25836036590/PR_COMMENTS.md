# Add `add_by_HRA` subset tag

Closes #3590.

## Summary

Adds a new subset annotation property `cl:add_by_HRA` to `src/ontology/cl-edit.owl` so the HRA team can tag and track terms added by HRA and HuBMAP team members.

## Changes

A single change in `src/ontology/cl-edit.owl`:

1. Added a `Declaration(AnnotationProperty(cl:add_by_HRA))` alongside the other subset-property declarations.
2. Added the property definition block:
   - `rdfs:label` = `"add_by_HRA"`
   - `rdfs:comment` describing it as a subset tag tracking terms added by HRA / HuBMAP team members
   - `terms:date` timestamp
   - `SubAnnotationPropertyOf(cl:add_by_HRA oboInOwl:SubsetProperty)` so it is recognised as a subset

The pattern mirrors the existing `cl:added_for_HCA` (and other subset tags such as `cl:BDS_subset`, `cl:blood_and_immune_upper_slim`, etc.) already declared in the file. The new property is placed alphabetically between `cl:BDS_subset` and `cl:added_for_HCA`.

## Rationale

The issue requested adding a subset tag named `add_by_HRA`. I kept the exact name the issue specified rather than renaming it (e.g. to `added_by_HRA`) to match the requester's wording. Once declared, terms can be tagged with this subset by adding axioms of the form:

```
AnnotationAssertion(oboInOwl:inSubset obo:CL_xxxxxxx cl:add_by_HRA)
```

No terms have been tagged with this subset as part of this PR — the issue only asked to make the tag available. Tagging existing/future terms can be done in follow-up edits.

## Checklist

- [x] Read the issue and confirmed scope (declare a new subset tag; no term tagging requested)
- [x] Inspected existing subset properties (`cl:BDS_subset`, `cl:added_for_HCA`, `cl:blood_and_immune_upper_slim`, etc.) to match conventions
- [x] Added `Declaration` and `SubAnnotationPropertyOf oboInOwl:SubsetProperty`
- [x] Added `rdfs:label`, `rdfs:comment`, and `terms:date` to document the new property
- [x] Used the exact spelling `add_by_HRA` as requested in the issue
- [x] Preserved alphabetical-ish ordering of the subset-property declarations
- [ ] `robot reason` validation — `robot` was not available in this environment, so syntactic validation was performed by inspection only. Please re-run CI checks.

GitHub Copilot

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25836036590)
