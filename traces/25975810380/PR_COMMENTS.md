# Add abbreviation synonyms PBMC, WBC, RPE

Closes #3559.

## Summary

Adds three commonly used abbreviations as exact synonyms to existing CL terms, so that downstream ontology-mapping pipelines (such as the LLM-based mapping described in the issue) can match these strings:

| Term | Synonym added | Scope | Type |
|---|---|---|---|
| CL:2000001 peripheral blood mononuclear cell | `PBMC` | exact | abbreviation (`OMO:0003000`) |
| CL:0000738 leukocyte | `WBC` | exact | abbreviation (`OMO:0003000`) |
| CL:0002586 retinal pigment epithelial cell | `RPE` | exact | abbreviation (`OMO:0003000`) |

Each term also got a `term_tracker_item` (`IAO:0000233`) link back to issue #3559.

## Rationale

- The issue reporter observed that `PBMC`, `WBC`, and `RPE` are common strings extracted from sample metadata but were not mappable to CL because they were not registered as synonyms.
- @addiehl explicitly approved `PBMC` and `WBC` as exact synonyms.
- @scheuerm confirmed `RPE` is an appropriate synonym for retinal pigment epithelial cell; following the maintainer's lead on `PBMC`/`WBC` and the prevailing CL pattern for abbreviated cell-type acronyms (e.g. CL:4023059 "COP", CL:4030035 "DPSC", CL:4030032 "VIC"), I also added it as an exact synonym.
- All three abbreviations are unambiguous and standard in the cell biology / immunology / ophthalmology literature.

## Checklist

- [x] Confirmed each term exists at the IDs given in the issue (CL:2000001, CL:0000738, CL:0002586) and the labels match.
- [x] Confirmed no existing `PBMC` / `WBC` / `RPE` synonyms in `cl-edit.owl` (no duplicates introduced).
- [x] Followed existing axiom style for abbreviation synonyms — `hasExactSynonym` with `hasSynonymType OMO:0003000` annotation — already in use across the file.
- [x] Added `term_tracker_item` (`IAO:0000233`) on each edited term, linking back to issue #3559.
- [x] Did not add `dc:creator` or `dc:date` since these are edits to existing terms (per repo CLAUDE.md).
- [x] Edits limited to `src/ontology/cl-edit.owl`.

## Notes

- No logical axioms changed; this is a synonyms-only edit, so reasoner output is unaffected.
- No PMID is cited on the synonyms themselves because the abbreviations are not term-defining and the issue/maintainer approvals serve as provenance via the `term_tracker_item` link. Happy to add literature xrefs on request.

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25975810380)
