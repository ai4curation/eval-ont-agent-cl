Added the missing class `CL_0020022` (`prehypertrophic chondrocyte`) to `src/ontology/cl-edit.owl`.

Rationale:
- The imported issue requested a new term, but OLS already shows `CL:0020022 prehypertrophic chondrocyte` as an assigned CL class.
- This local checkout did not yet contain that class, so the correct fix here was to add the existing term rather than mint a duplicate ID.
- I used the issue text and cited literature to populate the definition and synonym, and linked the class back to issue `#3460`.

What changed:
- Added `Declaration(Class(obo:CL_0020022))`.
- Added the class stanza for `prehypertrophic chondrocyte`.
- Added definition xrefs: `PMID:29985449`, `PMID:31871141`, `PMID:34137454`.
- Added related synonym `preHTC` with abbreviation synonym type and PMID support.
- Added `terms:contributor` for the supplied ORCID.
- Added `terms:creator "GitHub Copilot"` and `terms:date` metadata.
- Added issue tracking annotation to `https://github.com/obophenotype/cell-ontology/issues/3460`.
- Placed the class under `CL_1000217` (`growth plate cartilage chondrocyte`).

Notes on modeling:
- I did not add a `part_of` restriction to a prehypertrophic zone term because I could confirm a hypertrophic-zone UBERON class in OLS, but not a corresponding prehypertrophic-zone anatomy term to use safely here.
- The chosen parent is therefore conservative and consistent with the issue description and existing growth-plate chondrocyte modeling.

Validation and review performed:
- Read the imported issue context from `__issue_context__.json`.
- Checked the local ontology file to confirm the term was absent.
- Verified via OLS API that `CL:0020022 prehypertrophic chondrocyte` already exists upstream.
- Reviewed nearby chondrocyte terms and local metadata conventions before editing.
- Inspected the resulting diff before commit.

Checklist completed:
- [x] Read issue context
- [x] Confirmed the requested term was not already present in this checkout
- [x] Confirmed the assigned CL ID upstream in OLS
- [x] Added the missing class with references and metadata
- [x] Committed only the ontology file change

Local commit:
- `ff75244` `Add CL_0020022 prehypertrophic chondrocyte`

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25977204124)
