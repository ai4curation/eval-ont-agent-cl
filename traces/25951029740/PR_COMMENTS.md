## Summary

This PR removes local relabeling of imported `oboInOwl:*` annotation properties from `src/ontology/cl-edit.owl`. These labels are already supplied by the merged import module, and keeping local copies in `cl-edit.owl` can create unstable serialization diffs when imported labels conflict.

It also adds guidance to `docs/annotation_properties.md` instructing editors not to add local `rdfs:label` annotations for imported `oboInOwl:*` properties, with the exact `rg` check to run before committing.

## Checklist

- [x] Read issue #3588 context from `__issue_context__.json`.
- [x] Removed local `rdfs:label` assertions for imported `oboInOwl:*` annotation properties.
- [x] Removed the generated comment blocks for those local label assertions.
- [x] Added documentation for the recurrence check.
- [x] Verified `rg 'AnnotationAssertion\(rdfs:label oboInOwl:' src/ontology/cl-edit.owl` returns no results.
- [x] Ran `git diff --check` on the edited files.

## Validation

The targeted `rg` check now returns no matches, confirming that no local `oboInOwl:*` annotation property labels remain in `cl-edit.owl`.

`git diff --check -- src/ontology/cl-edit.owl docs/annotation_properties.md` completed without errors.

I attempted to run `robot convert -i src/ontology/cl-edit.owl -o /tmp/cl-edit.owl`, but `robot` is not installed in this environment.

Signed-off-by: GitHub Copilot

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.5`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951029740)
