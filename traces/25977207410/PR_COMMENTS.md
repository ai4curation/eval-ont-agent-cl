Updated the five existing otic fibrocyte subclasses in `src/ontology/cl-edit.owl` to align with issue #3408.

What changed:
- Relabeled the existing subclasses to the spiral ligament fibrocyte naming scheme:
  - `CL_0002670` -> `type I spiral ligament fibrocyte`
  - `CL_0002666` -> `type II spiral ligament fibrocyte`
  - `CL_0002669` -> `type III spiral ligament fibrocyte`
  - `CL_0002668` -> `type IV spiral ligament fibrocyte`
  - `CL_0002667` -> `type V spiral ligament fibrocyte`
- Kept the pre-existing Roman numeral otic names as exact synonyms and added the Arabic-number otic names as broad synonyms, per the issue request.
- Rewrote the definitions using the issue-provided text and added the requested PMIDs without removing the older references already on the terms.
- Reparented these subclasses from the broader `otic fibrocyte` term to `spiral ligament fibrocyte` (`CL_0020005`).
- Added available anatomical restrictions from the issue where I could ground them safely:
  - `part of some spiral ligament` for all five subclasses
  - `adjacent to some stria vascularis of cochlear duct` for type I and type V
  - `adjacent to some spiral prominence of cochlear duct` for type II
  - `adjacent to some otic capsule` for type III
- Added issue tracker annotations linking each edited term back to issue #3408.
- Added the requested exact synonym `tension fibroblast` to type III.

Modeling notes:
- The imported issue title says `type I-IV`, but the body contains explicit requested updates for type V as well. I followed the detailed body and updated all five existing subclasses.
- I did not add an additional logical axiom for type IV beyond `part of some spiral ligament`, because the issue text did not provide a stable target anatomy ID I could verify locally for `crista basilaris`/`basilar crest`.

Validation performed:
- Ran `robot convert -i src/ontology/cl-edit.owl -o /tmp/opencode/cl-edit.ofn` to confirm the edited ontology still parses.

Completed checklist:
- [x] Read the imported issue context
- [x] Inspected the existing fibrocyte terms in `cl-edit.owl`
- [x] Updated labels, synonyms, definitions, and references
- [x] Added available anatomical axioms
- [x] Validated ontology syntax with ROBOT
- [x] Committed only the edited ontology file

---
🤖 **Generated by opencode agent**
- Runtime: `opencode`
- Model: `openai/gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25977207410)
