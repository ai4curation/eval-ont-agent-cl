Updated `CL_0000743` (`hypertrophic chondrocyte`) in `src/ontology/cl-edit.owl` to align the term with the issue request and current literature.

Completed checklist:
- Re-read the imported issue context and scoped the edit to `src/ontology/cl-edit.owl`.
- Confirmed the existing term block for `CL_0000743`.
- Verified the requested anatomy target as `UBERON:0008187` (`hypertrophic cartilage zone`).
- Verified the requested process target as `GO:0001958` (`endochondral ossification`).
- Reviewed the cited literature context for `PMID:35179487` and `PMID:41207902`, and used the issue-provided `PMID:25321476` in the revised definition xrefs.
- Checked the local term block for a `present in taxon Homo sapiens` axiom and found none to remove.
- Committed only the ontology edit.

Changes made:
- Replaced the old textual definition that described hypertrophic chondrocytes as terminally differentiated.
- Added a new definition stating that the cell is part of the hypertrophic cartilage zone, is enlarged, expresses type X collagen, participates in endochondral ossification, and can survive to dedifferentiate into skeletal stem/progenitor cells that later give rise to osteoblasts and osteocytes.
- Added `SubClassOf(obo:CL_0000743 ObjectSomeValuesFrom(obo:BFO_0000050 obo:UBERON_0008187))`.
- Added `SubClassOf(obo:CL_0000743 ObjectSomeValuesFrom(obo:RO_0002215 obo:GO_0001958))`.
- Added a local `Declaration(Class(obo:UBERON_0008187))` so the referenced Uberon class follows the repository's existing pattern for locally declared imported anatomy terms.

Rationale:
- The previous definition was outdated because it encoded the older view that hypertrophic chondrocytes are terminally differentiated.
- The revised definition reflects the lineage-tracing evidence cited in the issue that these cells can persist and contribute to osteoblast/osteocyte lineages through dedifferentiated progenitor states.
- The new logical axioms capture the requested anatomy and process associations explicitly.

Validation:
- Inspected the resulting diff to confirm the edit scope.
- `robot` validation could not be run in this environment because the `robot` command is not installed.
- The publication helper mentioned in the repo instructions, `aurelian`, is also not installed here, so literature verification used accessible web sources instead.

---
🤖 **Generated by codex agent**
- Runtime: `codex`
- Model: `gpt-5.4`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/25951044625)
