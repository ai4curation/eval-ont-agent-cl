## Summary

Adds three abbreviation synonyms to existing Cell Ontology terms in response to issue #3559.

| Term | ID | Abbreviation | Scope |
|------|----|--------------|-------|
| peripheral blood mononuclear cell | CL:2000001 | PBMC | exact |
| leukocyte | CL:0000738 | WBC | exact |
| retinal pigment epithelial cell | CL:0002586 | RPE | exact |

All three synonyms are annotated with the `abbreviation` synonym type (`OMO:0003000`), consistent with existing CL convention for abbreviation synonyms (e.g., D2-MSN on CL:4023029, GBC on CL:4023164, COP on CL:4023059).

## Rationale

The issue requester noted that PBMC, WBC, and RPE were strings extracted from experimental sample metadata that failed to map to the Cell Ontology. Adding the abbreviations as synonyms allows downstream tools (such as LLM-driven ontology mappers) to resolve these common forms.

- PBMC and WBC were confirmed as appropriate for exact synonyms by @addiehl.
- RPE was confirmed as appropriate by @scheuerm.

All three abbreviations are well-established, unambiguous mappings to the corresponding cell type concepts in the biomedical literature, so the `exact` synonym scope is appropriate (rather than `related`).

## Checklist

- [x] Read issue #3559 and the comments from @addiehl and @scheuerm
- [x] Verified all three CL term IDs exist and confirmed their labels
- [x] Checked existing usage of `OMO:0003000` (abbreviation) synonym type for consistency
- [x] Confirmed pattern for `hasExactSynonym` with `hasSynonymType OMO:0003000` is already in use in cl-edit.owl
- [x] Added synonyms in the appropriate position within each term's axiom block
- [x] No logical axioms changed; this is a metadata-only edit

## Validation

- Only `src/ontology/cl-edit.owl` was modified.
- Three new annotation assertions added; existing axioms unchanged.
- No new term IDs created (these are existing terms).

Signed-off-by: GitHub Copilot

---
🤖 **Generated by claude agent**
- Runtime: `claude`
- Model: `claude-opus-4-7`
- Agent config: `ai4curation/cl-agent-config@v3:.`
- Iteration: `1`
- Run: [View workflow run](https://github.com/ai4curation/eval-ont-agent-cl/actions/runs/26011797724)
